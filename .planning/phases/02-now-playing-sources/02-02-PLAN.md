---
phase: 02-now-playing-sources
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/sources/chain.ts
  - scripts/test-sources.ts
  - package.json
autonomous: true
requirements:
  - SRC-02
  - SRC-03
  - SRC-05
tags: [typescript, esm, source-chain, composer, smoke-test, applescript-authority]

must_haves:
  truths:
    - "getNowPlaying() liefert null, wenn AppleScript-Adapter state=null (Music.app nicht installiert/läuft) UND Last.fm null liefert"
    - "getNowPlaying() liefert null, wenn AppleScript state='paused' oder 'stopped' meldet — auch wenn Last.fm noch einen nowplaying-Track liefert (SRC-03 Authority)"
    - "getNowPlaying() bevorzugt Last.fm-Daten, wenn AppleScript state='playing' UND Last.fm einen Track liefert (NepTunes hat oft saubere Metadaten)"
    - "getNowPlaying() fallt auf AppleScript-np zurück, wenn AppleScript state='playing' aber Last.fm null liefert (SRC-02)"
    - "Einzelne Source-Fehler crashen den Composer nicht — try/catch in beiden Adaptern ist Schutz; Composer hat zusätzlichen Top-Level-Guard (SRC-05)"
    - "scripts/test-sources.ts gibt JSON-Output für 3 separate Calls (Last.fm, AppleScript, Chain) zur manuellen Verifikation aus"
  artifacts:
    - path: "src/sources/chain.ts"
      provides: "getNowPlaying-Composer mit AppleScript-as-Authority-Logic"
      exports: ["getNowPlaying"]
    - path: "scripts/test-sources.ts"
      provides: "Manuelles Smoke-Test-Script (kein Blocker, User-Verifikation)"
      contains: "getLastFmNowPlaying"
    - path: "package.json"
      provides: "test:sources-Script-Eintrag"
      contains: "test:sources"
  key_links:
    - from: "src/sources/chain.ts"
      to: "src/sources/applescript.ts"
      via: "getAppleScriptState() — Authority-Check vor Last.fm"
      pattern: "getAppleScriptState"
    - from: "src/sources/chain.ts"
      to: "src/sources/lastfm.ts"
      via: "getLastFmNowPlaying() — wenn AppleScript state=playing"
      pattern: "getLastFmNowPlaying"
    - from: "scripts/test-sources.ts"
      to: "src/sources/chain.ts + lastfm.ts + applescript.ts"
      via: "drei separate Calls für Diff-Beobachtung"
      pattern: "getNowPlaying.*getLastFmNowPlaying.*getAppleScriptState"
---

<objective>
Source-Chain-Composer (`getNowPlaying`) implementieren, der die zwei Adapter aus Plan 02-01 zu einem einzigen Vertrag zusammenführt: AppleScript ist Authority für Play/Pause/Stop, Last.fm liefert die Metadaten, wenn beide übereinstimmen. Plus ein optionales `scripts/test-sources.ts` Smoke-Test-Script, das alle drei Funktionen einzeln aufruft und JSON-Output für die manuelle Verifikation gegen echtes Apple Music + NepTunes ausgibt.

Purpose: Der Composer ist der einzige Entry-Point, den Phase 3 (Polling-Loop) sehen wird. Phase 3 ruft alle 10s `await getNowPlaying()` auf und wirft das Ergebnis in den GatherSink. Die ganze Source-Chain-Komplexität (Authority-Logik, Fallback, Error-Handling) ist hier gekapselt. Plan-Test-Script ist optional und automatisierbar — wenn der User Apple Music + NepTunes aktiv hat, läuft `npm run test:sources` ohne weiteres Setup, sonst ist es eine reine User-Action.

Output: `src/sources/chain.ts` mit `getNowPlaying`, `scripts/test-sources.ts` als Smoke-Test, neuer npm-Script-Eintrag `test:sources`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-now-playing-sources/02-CONTEXT.md
@.planning/phases/02-now-playing-sources/02-01-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md

@src/types.ts
@src/logger.ts
@package.json

<interfaces>
<!-- Aus Plan 02-01 verfügbar: -->

From src/sources/types.ts:
```typescript
import type { NowPlaying, PlayerState } from "../types.js";

export type NowPlayingSource = () => Promise<NowPlaying>;

export type AppleScriptResult = {
  state: PlayerState;        // "playing" | "paused" | "stopped" | null
  np: NowPlaying;            // {artist, track} | null
};
```

From src/sources/lastfm.ts:
```typescript
export async function getLastFmNowPlaying(): Promise<NowPlaying>;
// Wirft NIE. Bei jedem Fehler: log.warn + return null.
```

From src/sources/applescript.ts:
```typescript
export async function getAppleScriptState(): Promise<AppleScriptResult>;
// Wirft NIE. Bei jedem Fehler: log.warn + return { state: null, np: null }.
//
// Vertrags-Garantien:
// - state===null:                Music.app nicht installiert/läuft ODER AppleScript-Fehler. np=null.
// - state==="playing":            np ist {artist, track} aus Music.app (kann von Last.fm überschrieben werden).
// - state==="paused"|"stopped":   np=null. Authority überschreibt Last.fm-stale.
```

From src/types.ts:
```typescript
export type NowPlaying = { artist: string; track: string; } | null;
export type PlayerState = "playing" | "paused" | "stopped" | null;
```

From src/logger.ts:
```typescript
export const log: pino.Logger;
```

Source-Chain-Pseudo-Code (aus 02-CONTEXT.md `<specifics>`, übernehmen 1:1):

```
async getNowPlaying() {
  const appleState = await getAppleScriptState();

  if (appleState.state === null) {
    // Music.app nicht installiert/läuft → fallback auf Last.fm allein
    return await getLastFmNowPlaying();
  }

  if (appleState.state !== "playing") {
    // Pause/Stop → Authority sagt: nichts spielt
    return null;
  }

  // Music.app spielt: Last.fm bevorzugen wegen sauberer Metadaten
  const lastfm = await getLastFmNowPlaying();
  if (lastfm) return lastfm;

  // Last.fm hat nichts (NepTunes-Lag oder offline) → AppleScript-np
  return appleState.np;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: src/sources/chain.ts — Source-Chain-Composer mit AppleScript-as-Authority-Logic</name>
  <files>src/sources/chain.ts</files>
  <action>
    Erstelle `src/sources/chain.ts`. Die Logik folgt dem Pseudo-Code aus 02-CONTEXT.md `<specifics>` exakt — keine Vereinfachungen, keine Reorderings.

    **Vollständige Implementation:**

    ```typescript
    import { log } from "../logger.js";
    import type { NowPlaying } from "../types.js";
    import type { NowPlayingSource } from "./types.js";
    import { getLastFmNowPlaying } from "./lastfm.js";
    import { getAppleScriptState } from "./applescript.js";

    /**
     * Source-Chain-Composer (SRC-02, SRC-03, SRC-05).
     *
     * Reihenfolge der Auflösung:
     *
     * 1. AppleScript fragt zuerst nach Player-State (= Authority für Play/Pause/Stop, SRC-03).
     *    - state===null:      Music.app nicht installiert/läuft → fallback nur auf Last.fm.
     *    - state==="paused"/"stopped": → return null. Last.fm ist hier IRRELEVANT, weil
     *      NepTunes vergessen kann, den nowplaying-Status zu canceln (Pitfall 10).
     *
     * 2. Music.app spielt aktiv: Last.fm bevorzugen, weil NepTunes oft saubere Metadaten
     *    liefert (Künstler-Splits, Featured Artists). Fallback auf AppleScript-Daten,
     *    wenn Last.fm gerade null liefert (NepTunes-Lag von 5-15s nach Track-Wechsel,
     *    Pitfall 9).
     *
     * Vertrag (SRC-05): wirft NIE. Beide Adapter mappen Errors intern auf null/Result.
     * Zusätzlicher Top-Level-Guard nur als Belt-and-suspenders gegen unerwartete
     * Throws (z. B. Out-of-Memory, importfehler in Sub-Modulen).
     */
    export const getNowPlaying: NowPlayingSource = async () => {
      try {
        // Step 1: AppleScript-Authority abfragen
        const appleState = await getAppleScriptState();

        if (appleState.state === null) {
          // Music.app läuft nicht (Outer-Guard) ODER AppleScript-Fehler.
          // → fallback nur auf Last.fm; wenn auch das null liefert, ist Status leer.
          // Begründung für Fallback: User könnte Music.app geschlossen haben, aber
          // Spotify/Browser-Audio läuft und NepTunes scrobbelt das. Sehr selten,
          // aber kein Schaden, weil AppleScript-Authority hier nicht verfügbar ist.
          return await getLastFmNowPlaying();
        }

        if (appleState.state !== "playing") {
          // Authority sagt: pausiert/gestoppt → Status leeren.
          // Last.fm wird BEWUSST nicht abgefragt — siehe Pitfall 10:
          // NepTunes löscht den nowplaying-Eintrag nicht aktiv, der bleibt für
          // bis zu 10min stale. AppleScript ist die Wahrheit.
          return null;
        }

        // appleState.state === "playing"
        // Step 2: Last.fm-Metadaten bevorzugen, AppleScript-np als Fallback.
        const fromLastFm = await getLastFmNowPlaying();
        if (fromLastFm) {
          return fromLastFm;
        }
        return appleState.np;
      } catch (err) {
        // Belt-and-suspenders: beide Adapter werfen by contract NIE.
        // Wenn wir hier landen, ist etwas Unerwartetes passiert (z. B. Modul-Load-Error
        // bei dynamischem Import, Out-of-Memory). Loggen + null zurückgeben statt
        // den Caller (Polling-Loop in Phase 3) crashen zu lassen.
        log.error({ err }, "[chain] unexpected error in getNowPlaying — returning null");
        return null;
      }
    };
    ```

    Hinweise:
    - `export const getNowPlaying: NowPlayingSource = async () => { ... }` typt die Funktion explizit gegen das `NowPlayingSource`-Interface aus Task 2 von Plan 02-01. Wenn jemand später aus Versehen eine andere Signatur returniert, schlägt der TS-Check zu.
    - Pseudo-Code aus 02-CONTEXT.md ist 1:1 übernommen. Keine zusätzlichen Optimierungen (Caching, parallele Last.fm + AppleScript per `Promise.all`) — bewusste Single-Pass-Sequence, weil die Authority-Logik die Reihenfolge braucht.
    - Top-Level-`try/catch` ist BELT-AND-SUSPENDERS, NICHT Pflicht — die Adapter wirken bereits nicht. Aber es kostet 3 Zeilen und schützt gegen exotische Imports/Out-of-Memory-Cases. SRC-05 wird damit auf jeder Layer durchgesetzt.
    - Imports nutzen `.js`-Endung (NodeNext, Pitfall 21). `import type` für rein-typseitige Imports (vermeidet überflüssige Runtime-Side-Effects).
    - Kein `import "../setup-ws.js"` — Sources laufen ohne WebSocket-Polyfill (das ist Sink-only).

    Atomic-Commit: `feat(02-02): add source-chain composer with AppleScript-as-authority logic`.
  </action>
  <verify>
    <automated>npx tsc -p . --noEmit && grep -q "export const getNowPlaying: NowPlayingSource" src/sources/chain.ts && grep -q "getAppleScriptState" src/sources/chain.ts && grep -q "getLastFmNowPlaying" src/sources/chain.ts && grep -q 'state === null' src/sources/chain.ts && grep -q 'state !== "playing"' src/sources/chain.ts && echo OK</automated>
  </verify>
  <done>
    `src/sources/chain.ts` exportiert `getNowPlaying: NowPlayingSource`. Implementierung folgt dem Pseudo-Code aus 02-CONTEXT.md exakt: 1) AppleScript-State holen, 2) bei state===null nur Last.fm, 3) bei state!=="playing" return null, 4) sonst Last.fm bevorzugen, AppleScript-np als Fallback. Top-Level-try/catch loggt unerwartete Errors und returned null. `npx tsc -p . --noEmit` ist clean. Atomic-Commit ist gesetzt.
  </done>
</task>

<task type="auto">
  <name>Task 2: scripts/test-sources.ts + npm test:sources Script — Manuelles Smoke-Test-Script</name>
  <files>scripts/test-sources.ts, package.json</files>
  <action>
    **2a) Erstelle `scripts/test-sources.ts`:**

    Ein TypeScript-Script unter `scripts/`, das via `tsx` ausführbar ist (siehe Phase-1-Pattern in `scripts/test-sink.ts`). Ruft alle drei Funktionen einzeln auf, gibt strukturierten JSON-Output aus, exitet mit 0.

    Zweck: User kann mit aktivem Apple Music + NepTunes verifizieren:
    - Last.fm liefert nowplaying-Track (`@attr.nowplaying === "true"`).
    - AppleScript erkennt Music.app als laufend und player state.
    - Chain liefert das richtige Ergebnis je nach Pause/Play-Zustand.

    Das Script ist KEIN Blocker — wenn der User keine Apple-Music-Session hat, läuft es trotzdem ohne Crash und gibt klar erkennbar `null`/`state: null` aus.

    **Vollständige Implementation:**

    ```typescript
    /**
     * Manuelles Smoke-Test-Script für Phase 2 Sources.
     *
     * Aufruf: `npm run test:sources`
     *
     * Output: drei JSON-Sektionen für Last.fm, AppleScript, Chain — strukturiert
     * für visuelle Verifikation gegen echtes Apple Music + NepTunes.
     *
     * Kein Crash, wenn:
     * - Music.app nicht läuft (AppleScript-Adapter liefert state=null)
     * - Last.fm-API-Key falsch (Adapter loggt + null)
     * - NepTunes nicht aktiv (Last.fm liefert null, AppleScript-Pfad greift)
     *
     * Verwendung für die Phase-2-Verifikation:
     * 1. Music.app pausieren → Chain muss null liefern (Authority-Test).
     * 2. Music.app spielen + NepTunes aktiv → Chain liefert Last.fm-Daten.
     * 3. Music.app spielen + NepTunes deaktiviert → Chain liefert AppleScript-Daten.
     * 4. Music.app komplett geschlossen → AppleScript state=null, Chain liefert
     *    Last.fm-Daten oder null. Music.app DARF NICHT durch das Script starten.
     */

    import { log } from "../src/logger.js";
    import { getLastFmNowPlaying } from "../src/sources/lastfm.js";
    import { getAppleScriptState } from "../src/sources/applescript.js";
    import { getNowPlaying } from "../src/sources/chain.js";

    async function main(): Promise<void> {
      log.info("=== Phase 2 Source Smoke Test ===");

      // 1. Last.fm pur
      log.info("[1/3] Last.fm direkt …");
      const lastfm = await getLastFmNowPlaying();
      log.info({ result: lastfm }, "[1/3] Last.fm result");

      // 2. AppleScript pur
      log.info("[2/3] AppleScript direkt …");
      const apple = await getAppleScriptState();
      log.info({ result: apple }, "[2/3] AppleScript result");

      // 3. Chain (Composer)
      log.info("[3/3] Source-Chain (Composer) …");
      const chain = await getNowPlaying();
      log.info({ result: chain }, "[3/3] Chain result");

      log.info("=== Done ===");
    }

    main().catch((err: unknown) => {
      log.fatal({ err }, "Smoke-Test crashed unexpectedly");
      process.exit(1);
    });
    ```

    Hinweise:
    - **Wichtig:** Das Script importiert `../src/sources/chain.js` etc. mit `.js`-Endung — relative Imports von `scripts/` nach `src/` brauchen denselben NodeNext-Style wie alles andere.
    - `tsx` resolved den `.js`-Suffix automatisch zur `.ts`-Source — kein extra Build-Step.
    - **Sicherheits-Pflicht:** Das Script DARF NICHT Music.app starten, wenn sie nicht läuft. Der Outer-Guard im AppleScript-Adapter sorgt dafür. Beim ersten Run (Music.app geschlossen + Music.app nie vom Daemon angesprochen) erscheint kein TCC-Prompt — der Script-Run failt einfach mit `state: null`. Sobald Music.app läuft, kommt der TCC-Prompt im Vordergrund (User muss klicken). Der Hint dazu wird in Phase 4 (DMN-03) im Install-Script professionell gehandhabt; in Phase 2 ist es ein erwarteter, einmaliger Flow.
    - **Exit-Code:** Bei normalem Lauf 0. Nur unerwartete Crashes (z. B. Importfehler) führen zu exit(1). Single-Run-Script — kein Daemon, kein KeepAlive-Effekt.
    - Logger statt `console.log` verwenden, damit die Redaction-Strategy aus Phase 1 (Pitfall 5) auch hier greift, falls jemand später einen Adapter um einen Key erweitert.

    **2b) `package.json` um `test:sources`-Script erweitern:**

    Füge in den `scripts`-Block (zwischen `test:sink` und vorhandenen Einträgen) den neuen Eintrag hinzu:

    ```json
    "test:sources": "tsx scripts/test-sources.ts"
    ```

    Komplettes `scripts`-Block-Beispiel danach (zur Orientierung; behalte alle bestehenden Einträge unverändert):

    ```json
    "scripts": {
      "dev": "tsx watch src/index.ts",
      "build": "tsc -p .",
      "start": "node dist/index.js",
      "typecheck": "tsc -p . --noEmit",
      "test:sink": "tsx scripts/test-sink.ts",
      "test:sources": "tsx scripts/test-sources.ts"
    }
    ```

    KEINE weiteren Änderungen an `package.json` — nur dieser eine Script-Eintrag.

    Atomic-Commits:
    - `feat(02-02): add manual smoke test script for source adapters` (für 2a)
    - `chore(02-02): add npm test:sources script` (für 2b)

    Zwei separate Commits, weil das Script und der package.json-Eintrag konzeptuell zwei getrennte Änderungen sind und einzeln revertierbar bleiben sollen.
  </action>
  <verify>
    <automated>npx tsc -p . --noEmit && test -f scripts/test-sources.ts && grep -q "getLastFmNowPlaying" scripts/test-sources.ts && grep -q "getAppleScriptState" scripts/test-sources.ts && grep -q "getNowPlaying" scripts/test-sources.ts && node -e "const p = require('./package.json'); if (p.scripts['test:sources'] !== 'tsx scripts/test-sources.ts') { console.error('FAIL'); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <done>
    `scripts/test-sources.ts` existiert und ruft alle drei Funktionen (`getLastFmNowPlaying`, `getAppleScriptState`, `getNowPlaying`) sequentiell auf mit pino-Logging der Ergebnisse. `package.json` hat den neuen Script-Eintrag `test:sources`. `npx tsc -p . --noEmit` ist clean. Beide Atomic-Commits sind gesetzt. (Live-Smoke-Run gegen Apple Music + NepTunes ist optional und wird vom User durchgeführt — kein Plan-Blocker.)
  </done>
</task>

</tasks>

<verification>
**Statische Verifikation (alle automatisch nach Plan-Abschluss):**

1. `npx tsc -p . --noEmit` clean (gesamt-Typecheck inkl. neuer chain.ts und test-sources.ts).
2. `grep -q "getNowPlaying" src/sources/chain.ts` matched.
3. Pseudo-Code-Treue: `grep -E "state === null|state !== \"playing\"" src/sources/chain.ts` matched beide Branches.
4. `package.json` `scripts` enthält `test:sources`.
5. Atomic-Commits im git log (3 Commits: chain.ts, test-sources.ts, package.json-script).

**Optionale User-Verifikation (kein Plan-Blocker):**
- User mit aktivem Apple Music + NepTunes:
  ```bash
  npm run test:sources
  ```
  Erwartung:
  - `[1/3] Last.fm result`: `{ artist, track }` wenn aktuell ein Track läuft, sonst `null`.
  - `[2/3] AppleScript result`: `{ state: "playing", np: { artist, track } }` wenn Music.app spielt.
  - `[3/3] Chain result`: `{ artist, track }` (Last.fm bevorzugt, AppleScript-Fallback).
- Pause-Test:
  - User pausiert Music.app, läuft Script erneut.
  - `[1/3] Last.fm`: kann immer noch nowplaying-Track liefern (NepTunes hat nicht gecanceld).
  - `[2/3] AppleScript`: `{ state: "paused", np: null }`.
  - `[3/3] Chain`: `null` (Authority überschreibt Last.fm).
- Music.app-Closed-Test:
  - User beendet Music.app vollständig, läuft Script erneut.
  - `[2/3] AppleScript`: `{ state: null, np: null }` (Outer-Guard).
  - **KRITISCH:** Music.app springt NICHT wieder auf — sonst ist der Outer-Guard kaputt (Pitfall 1).

**Goal-Backward-Truths-Check (Plan 02-01 + 02-02 zusammen):**
- ✅ SC1 (Last.fm `@attr.nowplaying`-Filter): Plan 02-01 Task 3.
- ✅ SC2 (Last.fm-Fail → AppleScript-Fallback): Plan 02-02 Task 1, Branch `if (fromLastFm) return fromLastFm; return appleState.np;`.
- ✅ SC3 (AppleScript paused/stopped überschreibt Last.fm): Plan 02-02 Task 1, Branch `if (appleState.state !== "playing") return null;`.
- ✅ SC4 (AppleScript-Outer-Guard): Plan 02-01 Task 4, `application process "Music" exists` im Script.
- ✅ SC5 (Try/Catch um jeden Source-Call): Plan 02-01 Task 3 + Task 4 (Adapter-Level), Plan 02-02 Task 1 (Composer-Level Top-Guard).
</verification>

<success_criteria>
- `src/sources/chain.ts` exportiert `getNowPlaying: NowPlayingSource`.
- Pseudo-Code aus 02-CONTEXT.md ist 1:1 implementiert (drei Branches: state===null, state!==playing, sonst Last.fm-bevorzugt).
- `scripts/test-sources.ts` existiert und ruft alle drei Funktionen einzeln auf mit JSON-Logging.
- `package.json` hat den `test:sources`-Script-Eintrag.
- `npx tsc -p . --noEmit` ist clean.
- 3 atomic Commits im git log.
- Phase 2 Success Criteria 1-5 (siehe ROADMAP.md) sind durch die Module gemeinsam mit Plan 02-01 abgedeckt — User-Verifikation ist optional.
</success_criteria>

<output>
Nach Abschluss: erstelle `.planning/phases/02-now-playing-sources/02-02-SUMMARY.md` mit Frontmatter (requires: 02-01-SUMMARY, provides: getNowPlaying als NowPlayingSource, affects: 03-* Polling-Loop), key-decisions (Pseudo-Code 1:1-Treue, kein parallel-Promise.all, Top-Level-try/catch als Belt-and-suspenders, separate Commits für Script vs package.json), Coverage Statement (SRC-01..05 vollständig erfüllt nach Phase-Plan-Abschluss), Next Phase Readiness (Phase 3 importiert `getNowPlaying` aus `src/sources/chain.js`, alle 10s aufrufen, return-Wert in `sink.setStatus(np)` oder `sink.clearStatus()` schicken).

**Wichtiger Vermerk im SUMMARY:** Die User-Smoke-Verifikation (Music.app starten + pausieren + schließen — drei Läufe) ist NICHT Teil dieses Plans und wird im Phase-2-Verifier als `human_verification`-Block aufgenommen, ähnlich wie SINK-04 in Phase 1.
</output>
