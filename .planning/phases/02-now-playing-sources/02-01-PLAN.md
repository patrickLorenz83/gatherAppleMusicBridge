---
phase: 02-now-playing-sources
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/types.ts
  - src/sources/types.ts
  - src/sources/lastfm.ts
  - src/sources/applescript.ts
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - SRC-03
  - SRC-04
  - SRC-05
tags: [typescript, esm, lastfm, applescript, sources, zod, run-applescript, fetch]

must_haves:
  truths:
    - "Last.fm-Adapter liefert {artist, track} aus user.getRecentTracks gefiltert per @attr.nowplaying === \"true\" (nicht per Position)"
    - "Last.fm-Adapter liefert null bei HTTP-Fehler, Zod-Parse-Fehler oder fehlendem nowplaying-Track"
    - "AppleScript-Adapter liefert null wenn Music.app nicht läuft (Outer-Guard via System Events) — startet Music.app niemals"
    - "AppleScript-Adapter liefert PlayerState='paused'/'stopped'/null wenn Music.app läuft, aber nicht aktiv spielt"
    - "AppleScript-Adapter liefert {state: 'playing', np: {artist, track}} wenn Music.app gerade einen Track abspielt"
    - "Beide Adapter mappen Errors auf null und loggen via pino — kein Throw nach oben (SRC-05)"
  artifacts:
    - path: "package.json"
      provides: "run-applescript@^7.1.0 als Runtime-Dep"
      contains: "run-applescript"
    - path: "src/types.ts"
      provides: "PlayerState-Type-Erweiterung"
      contains: "PlayerState"
    - path: "src/sources/types.ts"
      provides: "NowPlayingSource-Interface + AppleScriptResult-Helper-Type"
      exports: ["NowPlayingSource", "AppleScriptResult"]
    - path: "src/sources/lastfm.ts"
      provides: "Last.fm-Adapter mit native fetch + Zod-Schema"
      exports: ["getLastFmNowPlaying"]
    - path: "src/sources/applescript.ts"
      provides: "AppleScript-Adapter mit Outer-Guard + Player-State-Authority"
      exports: ["getAppleScriptState"]
  key_links:
    - from: "src/sources/lastfm.ts"
      to: "ws.audioscrobbler.com/2.0"
      via: "native fetch mit AbortSignal.timeout(5000)"
      pattern: "fetch\\(url,.*AbortSignal\\.timeout"
    - from: "src/sources/lastfm.ts"
      to: "@attr.nowplaying"
      via: "Zod-validierter Filter via tracks.find"
      pattern: "nowplaying.*===.*\"true\""
    - from: "src/sources/applescript.ts"
      to: "Music.app"
      via: "run-applescript mit System-Events-Outer-Guard"
      pattern: "application process \"Music\" exists"
    - from: "src/sources/applescript.ts"
      to: "src/logger.ts"
      via: "log.warn bei AppleScript-Errors (run-applescript wirft, wird gefangen)"
      pattern: "log\\.warn"
---

<objective>
Source-Adapter für Last.fm und Music.app/AppleScript implementieren, plus das gemeinsame `NowPlayingSource`-Interface. Beide Adapter sind in sich isolierte, fehler-sichere Funktionen, die ein einheitliches Vertrags-Shape zurückgeben — Last.fm: `NowPlaying | null`, AppleScript: ein Result-Objekt mit Player-State plus optionalem `NowPlaying`. Kein Composer hier — das macht Plan 02-02.

Purpose: Phase 2 liefert die zwei Datenquellen, aus denen die Source-Chain in Plan 02 ihren Output baut. Last.fm liefert die saubereren Metadaten (NepTunes-Scrobbles), AppleScript ist Authority für Play/Pause/Stop und schützt vor stale `nowplaying`-Status nach Pausen (Pitfall 10). Outer-Guard in AppleScript verhindert, dass der Daemon Music.app als Poltergeist startet (Pitfall 1 = SRC-04).

Output: `run-applescript@^7.1.0` als Runtime-Dep, `PlayerState` als Type-Erweiterung in `src/types.ts`, drei neue Module unter `src/sources/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-now-playing-sources/02-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation-und-gather-sink/01-01-SUMMARY.md

@src/types.ts
@src/config.ts
@src/logger.ts
@package.json
@tsconfig.json

<interfaces>
<!-- Diese Interfaces stehen aus Phase 1 zur Verfügung. Direkt importieren, keine Codebase-Exploration nötig. -->

From src/types.ts (existing, wird in Task 2 erweitert):
```typescript
export type NowPlaying = {
  artist: string;
  track: string;
} | null;
```

From src/config.ts:
```typescript
export type Config = {
  LASTFM_API_KEY: string;
  LASTFM_USER: string;
  GATHER_API_KEY: string;
  GATHER_SPACE_ID: string;
};
export const config: Config;  // module-load-time validated, exit(0) bei Fehlern
```

From src/logger.ts:
```typescript
export const log: pino.Logger;  // mit Redaction für *.GATHER_API_KEY, *.LASTFM_API_KEY, *.apiKey
```

External APIs (aus STACK.md verifiziert):

Last.fm endpoint (HTTP GET, kein Auth-Header):
```
https://ws.audioscrobbler.com/2.0/
  ?method=user.getrecenttracks
  &user=<LASTFM_USER>
  &api_key=<LASTFM_API_KEY>
  &format=json
  &limit=1
```

run-applescript@7 (ESM-only):
```typescript
import { runAppleScript } from "run-applescript";
const out: string = await runAppleScript(scriptString);
// wirft bei AppleScript-Syntax-Error oder TCC-Permission-Fehler (-1743)
```

ImportPath-Konvention (NodeNext, Pitfall 21): relative TS-Imports MÜSSEN mit `.js`-Endung enden, z. B. `import { log } from "../logger.js"` (Source-Datei ist `.ts`, emittierter Output ist `.js`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run-applescript@^7.1.0 als Runtime-Dependency installieren</name>
  <files>package.json, package-lock.json</files>
  <action>
    Installiere `run-applescript@^7.1.0` als Runtime-Dependency:

    ```bash
    npm install run-applescript@^7.1.0
    ```

    Erwartung nach Install:
    - `package.json` `dependencies` enthält `"run-applescript": "^7.1.0"` (alphabetisch eingereiht zwischen `pino` und `ws`).
    - `package-lock.json` ist aktualisiert.
    - Audit-Warnings können wie in Phase 1 dokumentiert ignoriert werden (Pitfall 18, Single-User-Tool ohne User-Inputs).

    Hinweise:
    - `run-applescript@7.x` ist ESM-only — passt zu unserem `"type": "module"`-Setup (siehe STACK.md Version Compatibility).
    - `run-applescript` hat 0 Runtime-Deps von sindresorhus; clean install.
    - KEINE weiteren Pakete installieren. Insbesondere KEIN Last.fm-npm-Paket — Last.fm geht roll-your-own per native `fetch` (siehe STACK.md Begründung „Roll-your-own statt npm-Paket").

    Atomic-Commit: `feat(02-01): add run-applescript@7 runtime dependency`.
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); if (!p.dependencies['run-applescript']) { console.error('FAIL: run-applescript missing'); process.exit(1); } if (!p.dependencies['run-applescript'].startsWith('^7.')) { console.error('FAIL: wrong version', p.dependencies['run-applescript']); process.exit(1); } console.log('OK', p.dependencies['run-applescript']);"</automated>
  </verify>
  <done>
    `package.json` listet `run-applescript: ^7.x.y` unter `dependencies`. `package-lock.json` ist aktualisiert. Atomic-Commit ist gesetzt.
  </done>
</task>

<task type="auto">
  <name>Task 2: PlayerState-Type in src/types.ts ergänzen + src/sources/types.ts mit Source-Interface</name>
  <files>src/types.ts, src/sources/types.ts</files>
  <action>
    **2a) `src/types.ts` erweitern (per D-„Optional: erweitere src/types.ts um PlayerState"):**

    Füge unter dem bestehenden `NowPlaying`-Export einen neuen Type-Export hinzu, ohne den bestehenden `NowPlaying`-Block zu ändern:

    ```typescript
    /**
     * Player-State von Music.app, ermittelt via AppleScript (SRC-03).
     *
     * - "playing":  aktiv abspielend
     * - "paused":   pausiert (Authority sagt: Status leeren, auch wenn Last.fm noch nowplaying meldet)
     * - "stopped":  gestoppt
     * - null:       Music.app läuft nicht (Outer-Guard hat geblockt) ODER AppleScript-Fehler
     *
     * In Phase 2 (SRC-03 + SRC-04) ist AppleScript Authority für Play/Pause/Stop —
     * dieser State entscheidet in der Source-Chain (Plan 02-02), ob Last.fm-Daten
     * überhaupt verwendet werden dürfen oder das Ergebnis hart auf `null` gesetzt wird.
     */
    export type PlayerState = "playing" | "paused" | "stopped" | null;
    ```

    Das `NowPlaying`-Type bleibt 1:1 wie in Phase 1.

    **2b) Neues File `src/sources/types.ts` anlegen:**

    Verzeichnis `src/sources/` muss erst erstellt werden (existiert noch nicht). Dann:

    ```typescript
    import type { NowPlaying, PlayerState } from "../types.js";

    /**
     * Gemeinsames Interface für Now-Playing-Sources (SRC-05).
     *
     * Eine Source-Funktion liefert immer ein `Promise<NowPlaying>`, also entweder
     * {artist, track} oder null. Sie wirft NICHT — alle Source-Errors werden intern
     * zu null gemappt + via pino geloggt. Der Caller (Source-Chain in Plan 02-02)
     * darf sich darauf verlassen, dass eine Source-Funktion nie crasht.
     *
     * Beispiel-Implementationen:
     * - getLastFmNowPlaying() in src/sources/lastfm.ts
     * - Helper-Wrapper im Composer (Plan 02-02), der getAppleScriptState auf NowPlaying reduziert
     */
    export type NowPlayingSource = () => Promise<NowPlaying>;

    /**
     * Result-Shape des AppleScript-Adapters (Task 4).
     *
     * Trägt zusätzlich zum Track-Info den Player-State, weil dieser Authority für
     * Play/Pause/Stop ist (SRC-03) und in der Source-Chain (Plan 02-02) gegen den
     * Last.fm-Wert priorisiert wird.
     *
     * Vertrag:
     * - state === null:        Music.app läuft nicht ODER AppleScript hat gefailed.
     *                          np ist immer null.
     * - state === "playing":   Music.app spielt aktiv. np ist {artist, track}
     *                          aus Music.app (kann von Last.fm-Daten in der Chain
     *                          überschrieben werden, weil NepTunes oft saubere Metadaten liefert).
     * - state === "paused"|"stopped": Music.app läuft, aber spielt nicht.
     *                          np ist immer null. Authority überschreibt
     *                          stale Last.fm-nowplaying in der Chain.
     */
    export type AppleScriptResult = {
      state: PlayerState;
      np: NowPlaying;
    };
    ```

    Hinweise:
    - Beide Imports nutzen `.js`-Endung (NodeNext-Pflicht, Pitfall 21).
    - `import type { ... }` statt `import { ... }`, weil rein typseitig (vermeidet Runtime-Side-Effects in den Source-Modulen).
    - Datei hat keine Runtime-Code, exportiert nur Types — `tsc --noEmit` ist der relevante Check.

    Atomic-Commits:
    - `feat(02-01): add PlayerState type for AppleScript authority` (für 2a)
    - `feat(02-01): add NowPlayingSource interface + AppleScriptResult helper` (für 2b)
  </action>
  <verify>
    <automated>npx tsc -p . --noEmit && grep -q "export type PlayerState" src/types.ts && grep -q "export type NowPlayingSource" src/sources/types.ts && grep -q "export type AppleScriptResult" src/sources/types.ts && echo OK</automated>
  </verify>
  <done>
    `src/types.ts` exportiert weiterhin `NowPlaying` UND zusätzlich `PlayerState`. `src/sources/types.ts` exportiert `NowPlayingSource` und `AppleScriptResult`. `npx tsc -p . --noEmit` ist clean. Beide Atomic-Commits sind gesetzt.
  </done>
</task>

<task type="auto">
  <name>Task 3: src/sources/lastfm.ts — Last.fm-Adapter (native fetch + Zod-Schema, error-zu-null)</name>
  <files>src/sources/lastfm.ts</files>
  <action>
    Erstelle `src/sources/lastfm.ts` als Last.fm-Adapter. Pflicht-Pattern aus STACK.md/PITFALLS.md:

    1. **Native `fetch`** (Node 22+) mit `AbortSignal.timeout(5000)` — kein npm-Paket, kein `node-fetch`, kein `axios`.
    2. **Zod-Schema** für defensives Parsing der Last.fm-Antwort (Pitfall „Last.fm-Response-Schluckauf"). Optional-Felder, wo Last.fm inkonsistent sein kann.
    3. **Filter via `@attr.nowplaying === "true"`** mit `tracks.find(...)`, NICHT `track[0]` — Pitfall 3 (= SRC-01).
    4. **Error-zu-null-Mapping** via try/catch um den gesamten fetch-/parse-Pfad. Bei jedem Fehler: `log.warn({ err }, "[lastfm] ...")` + `return null`. Kein Throw (SRC-05).
    5. **Last.fm liefert manchmal N+1 Tracks** bei `limit=1` mit aktivem nowplaying — `find` über das gesamte Array statt index-basiert (Pitfall 3).

    **Vollständige Implementation:**

    ```typescript
    import { z } from "zod";
    import { config } from "../config.js";
    import { log } from "../logger.js";
    import type { NowPlaying } from "../types.js";

    /**
     * Last.fm-Now-Playing-Adapter (SRC-01, SRC-05).
     *
     * Endpoint: user.getRecentTracks (HTTP GET, kein Auth-Header, api_key in Query).
     * Filter: @attr.nowplaying === "true" via tracks.find (NICHT track[0], Pitfall 3).
     * Fehlerbehandlung: try/catch wraps fetch + JSON-Parse + Zod-Parse → log.warn + return null.
     *
     * Wird in Phase 2 Plan 02 (Source-Chain) gemeinsam mit dem AppleScript-Adapter genutzt.
     * AppleScript ist Authority für Play/Pause/Stop (SRC-03); dieser Adapter liefert nur
     * die Metadaten, wenn überhaupt etwas spielt.
     *
     * Rate-Limit: Last.fm erlaubt 5 Calls/s/IP (gemittelt über 5min). 10s-Polling = 0.1 Calls/s,
     * extrem safe (siehe PITFALLS Integration Gotchas).
     *
     * Native fetch (Node 22+) nutzt undici keep-alive per Default — kein Agent nötig.
     */

    /**
     * Defensives Zod-Schema für `user.getRecentTracks`-Response.
     *
     * Gemacht für Last.fm-Schluckauf: leere track-Arrays, fehlende @attr-Objects,
     * unerwartete Felder. Alles, was nicht zwingend für unseren Use-Case ist,
     * ist optional — sonst zerschießt ein API-Schluckauf den Parse.
     *
     * Konkrete Beobachtungen aus PITFALLS:
     * - Currently-playing-Track hat KEIN `date`-Feld, alle anderen schon → date als optional
     * - @attr fehlt komplett, wenn der Track nicht nowplaying ist → optional
     * - track kann ein Objekt statt Array sein, wenn nur 1 Track existiert (alte Last.fm-Quirk) → wir akzeptieren beide Formen
     */
    const TrackSchema = z.object({
      name: z.string(),
      artist: z.object({ "#text": z.string() }),
      "@attr": z.object({ nowplaying: z.string() }).optional(),
      // date ist absichtlich nicht modelliert — wir nutzen es nicht
    });

    const RecentTracksResponseSchema = z.object({
      recenttracks: z.object({
        // Last.fm liefert manchmal Objekt statt Array bei einem einzigen Track:
        track: z.union([z.array(TrackSchema), TrackSchema]),
      }),
    });

    export async function getLastFmNowPlaying(): Promise<NowPlaying> {
      try {
        const url = new URL("https://ws.audioscrobbler.com/2.0/");
        url.search = new URLSearchParams({
          method: "user.getrecenttracks",
          user: config.LASTFM_USER,
          api_key: config.LASTFM_API_KEY,
          format: "json",
          limit: "1",
        }).toString();

        const res = await fetch(url, { signal: AbortSignal.timeout(5000) });
        if (!res.ok) {
          log.warn({ status: res.status }, "[lastfm] non-2xx response");
          return null;
        }

        const json: unknown = await res.json();
        const parsed = RecentTracksResponseSchema.parse(json);

        // Normalisieren: Array oder Single-Object → Array
        const tracksRaw = parsed.recenttracks.track;
        const tracks = Array.isArray(tracksRaw) ? tracksRaw : [tracksRaw];

        // SRC-01: Filter via @attr.nowplaying === "true", NICHT track[0] (Pitfall 3)
        const np = tracks.find((t) => t["@attr"]?.nowplaying === "true");
        if (!np) {
          // Kein nowplaying-Track in der Response — gerade läuft (laut Last.fm) nichts.
          // Kein Log-Spam: das ist der Normalfall, wenn Music.app pausiert ist.
          return null;
        }

        const artist = np.artist["#text"];
        const track = np.name;
        if (!artist || !track) {
          log.warn({ np }, "[lastfm] nowplaying track missing artist/track");
          return null;
        }
        return { artist, track };
      } catch (err) {
        // SRC-05: jeder Fehler (Netzwerk, Timeout, Zod-Parse, JSON) → log + null.
        // KEIN throw — der Caller (Source-Chain) muss sich darauf verlassen können,
        // dass diese Funktion nicht crasht.
        log.warn({ err }, "[lastfm] failed to fetch now-playing");
        return null;
      }
    }
    ```

    Hinweise zum Code:
    - `import { config }` triggert in `src/config.ts` die Module-Load-Time-Validation (Phase 1). Wenn die `.env` kaputt ist, exitet der Prozess in `loadConfig()` mit `exit(0)` lange vor dem ersten fetch-Call.
    - `config.LASTFM_API_KEY` und `config.LASTFM_USER` werden NICHT geloggt (Redaction-Pfad `*.LASTFM_API_KEY` greift bei `log.warn({ ... apiKey: config.LASTFM_API_KEY })` — wir loggen aber sowieso nur Status-Codes und Errors, keinen Key).
    - `URLSearchParams.toString()` URL-encoded Username automatisch — falls ein User irgendwann „ä/ö/ü" oder Leerzeichen im Last.fm-Username hat, kein Bruch.
    - `AbortSignal.timeout(5000)` ist seit Node 17.3 Standard, in Node 22+ stabil (siehe STACK.md).
    - Kein `import "../setup-ws.js"` — dieser Side-Effect ist NUR für den Gather-Sink relevant; Sources verwenden native fetch ohne WebSocket.
    - Relative Imports MIT `.js` (NodeNext, Pitfall 21).

    Atomic-Commit: `feat(02-01): add Last.fm now-playing source with @attr.nowplaying filter`.
  </action>
  <verify>
    <automated>npx tsc -p . --noEmit && grep -q "export async function getLastFmNowPlaying" src/sources/lastfm.ts && grep -q '"@attr"' src/sources/lastfm.ts && grep -q "nowplaying" src/sources/lastfm.ts && grep -q "AbortSignal.timeout" src/sources/lastfm.ts && grep -q "tracks.find" src/sources/lastfm.ts && grep -vE '^\s*(//|\*)' src/sources/lastfm.ts | grep -q "log.warn" && echo OK</automated>
  </verify>
  <done>
    `src/sources/lastfm.ts` exportiert `getLastFmNowPlaying`. Funktion nutzt native `fetch` mit 5s-Timeout, Zod-Validation, `tracks.find(t => t["@attr"]?.nowplaying === "true")` Filter und try/catch + `log.warn` + return null bei jedem Fehler. `npx tsc -p . --noEmit` ist clean. Atomic-Commit ist gesetzt.
  </done>
</task>

<task type="auto">
  <name>Task 4: src/sources/applescript.ts — AppleScript-Adapter mit Outer-Guard und Player-State</name>
  <files>src/sources/applescript.ts</files>
  <action>
    Erstelle `src/sources/applescript.ts` als AppleScript-Adapter gegen Music.app. Pflicht-Pattern:

    1. **Outer-Guard via `System Events`** (SRC-04, Pitfall 1): Erst `application process "Music" exists` prüfen, BEVOR `tell application "Music"` ausgeführt wird. Sonst startet Music.app ungewollt.
    2. **Player-State als Authority** (SRC-03): Wenn `player state is not playing`, kein Track-Lookup — nur State zurück. AppleScript überschreibt damit stale Last.fm-`nowplaying` in der Chain (Pitfall 10).
    3. **Single AppleScript-Call** (eine `runAppleScript`-Invocation, nicht zwei) — minimiert Round-Trip-Overhead und TCC-Permission-Surface.
    4. **Output-Format mit Tab-Separator** (Pitfall 15): `\t` statt `|||`, weil Track-Namen praktisch nie Tabs enthalten.
    5. **Error-zu-null-Mapping** (SRC-05): try/catch um `runAppleScript`. TCC-Permission-Errors (-1743) sind häufig beim ersten Run unter launchd; explizit erkennen und freundlich loggen.

    **Vollständige Implementation:**

    ```typescript
    import { runAppleScript } from "run-applescript";
    import { log } from "../logger.js";
    import type { AppleScriptResult } from "./types.js";
    import type { PlayerState } from "../types.js";

    /**
     * AppleScript-Adapter gegen Music.app (SRC-02, SRC-03, SRC-04, SRC-05).
     *
     * Outer-Guard (SRC-04, Pitfall 1):
     *   `tell application "System Events"` prüft `application process "Music" exists`,
     *   BEVOR `tell application "Music"` ausgeführt wird. Ohne diesen Guard würde
     *   `tell application "Music"` Music.app starten — der User schließt die App,
     *   der Daemon pollt 10s später, Music.app springt geisterhaft wieder auf.
     *
     * Authority-Pattern (SRC-03, Pitfall 10):
     *   Wenn Music.app pausiert/gestoppt ist, liefert dieser Adapter state="paused"/"stopped"
     *   plus np=null. In der Source-Chain (Plan 02-02) überschreibt das stale Last.fm-Data,
     *   wo NepTunes vergessen hat, den nowplaying-Status zu canceln.
     *
     * TCC-Permission (Pitfall 7 = Phase 4 DMN-03):
     *   Beim ersten Run unter launchd kann macOS einen Automation-Permission-Prompt
     *   zeigen, der im Hintergrund nicht erscheint. errno -1743 ("Not authorized to send
     *   Apple events") wird hier explizit erkannt und mit Hinweis auf System Settings
     *   geloggt. Phase 4's Install-Script triggert die Permission im Vordergrund (DMN-03).
     */

    /**
     * Output-Format des Scripts:
     *   "" (leerer String):       Music.app läuft nicht (Outer-Guard greift)
     *   "STATE:<state>":          Music.app läuft, aber player state != playing
     *                             (z. B. "STATE:paused", "STATE:stopped", "STATE:fast forwarding")
     *   "PLAY:<artist>\t<track>": Music.app spielt einen Track ab
     *
     * Tab als Separator (Pitfall 15): in Track-Namen praktisch nie vorhanden.
     * Erste Zeile-Indikator (STATE:/PLAY:) als Disambiguator.
     */
    const SCRIPT = `
    tell application "System Events"
      if not (exists application process "Music") then return ""
    end tell
    tell application "Music"
      set s to player state as string
      if s is not "playing" then return "STATE:" & s
      try
        set a to artist of current track
        set t to name of current track
      on error
        return "STATE:" & s
      end try
      return "PLAY:" & a & tab & t
    end tell
    `;

    /**
     * Wandelt einen Music.app-player-state-String in PlayerState um.
     *
     * Music.app kennt: playing | paused | stopped | fast forwarding | rewinding.
     * Für unsere Use-Case sind "fast forwarding" und "rewinding" wie "playing" zu
     * behandeln (User hört aktiv), aber Sicherheits-Default ist "paused" — Status
     * wird in der Chain auf null gesetzt, was korrekter ist als ein Track anzuzeigen,
     * der gerade übersprungen wird.
     *
     * Wir mappen also strikt nur die drei "stabilen" States; alles andere → "paused".
     */
    function parsePlayerState(raw: string): PlayerState {
      switch (raw) {
        case "playing":
          return "playing";
        case "paused":
          return "paused";
        case "stopped":
          return "stopped";
        default:
          // fast forwarding, rewinding, oder unbekannt → vorsichtig "paused"
          return "paused";
      }
    }

    export async function getAppleScriptState(): Promise<AppleScriptResult> {
      let out: string;
      try {
        out = await runAppleScript(SCRIPT);
      } catch (err: unknown) {
        // SRC-05: jeder AppleScript-Error → log + Result mit state=null.
        const message = err instanceof Error ? err.message : String(err);
        if (message.includes("-1743") || message.includes("Not authorized")) {
          log.warn(
            { err },
            "[applescript] TCC permission denied — open System Settings → Privacy → Automation → Node → Music",
          );
        } else {
          log.warn({ err }, "[applescript] script execution failed");
        }
        return { state: null, np: null };
      }

      const trimmed = out.trim();

      // Outer-Guard hat zugeschlagen: Music.app läuft nicht.
      if (trimmed === "") {
        return { state: null, np: null };
      }

      if (trimmed.startsWith("STATE:")) {
        const raw = trimmed.slice("STATE:".length);
        return { state: parsePlayerState(raw), np: null };
      }

      if (trimmed.startsWith("PLAY:")) {
        const payload = trimmed.slice("PLAY:".length);
        // Erstes Tab teilt Artist/Track. indexOf statt split für Robustheit:
        // wenn der Track-Name (extrem selten) ein Tab enthält, bleibt es im Track-Teil.
        const tabIdx = payload.indexOf("\t");
        if (tabIdx === -1) {
          log.warn({ out }, "[applescript] PLAY-line missing tab separator");
          return { state: null, np: null };
        }
        const artist = payload.slice(0, tabIdx).trim();
        const track = payload.slice(tabIdx + 1).trim();
        if (!artist || !track) {
          log.warn({ artist, track }, "[applescript] empty artist or track");
          return { state: null, np: null };
        }
        return { state: "playing", np: { artist, track } };
      }

      log.warn({ out }, "[applescript] unexpected output format");
      return { state: null, np: null };
    }
    ```

    Hinweise:
    - `runAppleScript` ist die Default-Export-Funktion von `run-applescript@7.x` (siehe STACK.md AppleScript-Section).
    - Inline-AppleScript ist hier vertretbar (~15 Zeilen), `tab`-Builtin in AppleScript vermeidet Escaping-Probleme. Falls das Script später wächst (>20 Zeilen), wäre Auslagerung in `.applescript`-Datei der nächste Schritt — out of scope für v1.
    - TCC-Error-Pattern: `runAppleScript` wirft `Error` mit `-1743`-Substring im `.message`. Der Detect-Code ist defensiv (sucht beide Varianten — Code + Klartext-Substring).
    - `parsePlayerState` mappt unbekannte States bewusst auf `"paused"` (Sicherheits-Default), nicht `"playing"` — falls Apple Music einen neuen State erfindet, würde der Status leer bleiben statt einen falschen Track zu zeigen.
    - Imports nutzen `.js`-Endung für relative Pfade (NodeNext, Pitfall 21). `run-applescript` ist ein npm-Package → kein Suffix.
    - Kein `await` auf `runAppleScript` ohne try/catch außerhalb — der gesamte fehlerträchtige Pfad ist im try-Block.

    Atomic-Commit: `feat(02-01): add AppleScript source with System-Events outer-guard and player-state authority`.
  </action>
  <verify>
    <automated>npx tsc -p . --noEmit && grep -q "export async function getAppleScriptState" src/sources/applescript.ts && grep -q "System Events" src/sources/applescript.ts && grep -q 'application process "Music" exists' src/sources/applescript.ts && grep -q "player state" src/sources/applescript.ts && grep -q "1743" src/sources/applescript.ts && grep -vE '^\s*(//|\*)' src/sources/applescript.ts | grep -q "log.warn" && echo OK</automated>
  </verify>
  <done>
    `src/sources/applescript.ts` exportiert `getAppleScriptState`. Script enthält Outer-Guard `application process "Music" exists`, prüft `player state`, parst Output mit Tab-Separator. TCC-Error (-1743) wird explizit erkannt und mit Permission-Hinweis geloggt. Alle Errors → `{state: null, np: null}` ohne Throw. `npx tsc -p . --noEmit` ist clean. Atomic-Commit ist gesetzt.
  </done>
</task>

</tasks>

<verification>
**Statische Verifikation (alle automatisch nach Plan-Abschluss):**

1. `npx tsc -p . --noEmit` clean (kein Type-Fehler in src/types.ts, src/sources/types.ts, src/sources/lastfm.ts, src/sources/applescript.ts).
2. `grep -q "run-applescript" package.json` matched.
3. Alle 4 Atomic-Commits sind im `git log` (Task 1, 2a, 2b, 3, 4 — 5 Commits gesamt).
4. `node -e "import('./src/sources/lastfm.ts')..."` schlägt fehl (TS, kein Runtime-Direct-Run nötig — Adapter werden in Plan 02-02 vom Composer genutzt).

**End-to-End-Smoke-Test in diesem Plan:** keiner. Live-Tests passieren in Plan 02-02 via `scripts/test-sources.ts`. Hier ist der Plan-Output ein Set von **Bibliotheks-Modulen ohne Runtime-Entry**.

**Goal-Backward-Truths-Check:**
- ✅ SRC-01 (Last.fm-Filter via @attr.nowplaying): Task 3, `tracks.find(t => t["@attr"]?.nowplaying === "true")`.
- ✅ SRC-02 (AppleScript als Source): Task 4, vollständiger Adapter.
- ✅ SRC-03 (AppleScript Authority): Task 2 + Task 4 — `PlayerState`-Type plus `state`-Feld in `AppleScriptResult` plus `if s is not "playing" then return "STATE:" & s` im Script.
- ✅ SRC-04 (Outer-Guard): Task 4, `tell application "System Events" / if not (exists application process "Music") then return ""`.
- ✅ SRC-05 (Error-zu-null + Source-Interface): Task 2 (`NowPlayingSource`-Interface), Task 3 + Task 4 (jeweils try/catch + log.warn + return null/Result-mit-null).
</verification>

<success_criteria>
- `package.json` listet `run-applescript@^7.1.0` unter `dependencies`.
- `src/types.ts` exportiert `NowPlaying` UND `PlayerState`.
- `src/sources/types.ts` exportiert `NowPlayingSource` und `AppleScriptResult`.
- `src/sources/lastfm.ts` exportiert `getLastFmNowPlaying(): Promise<NowPlaying>` mit native fetch + Zod + @attr.nowplaying-Filter + try/catch.
- `src/sources/applescript.ts` exportiert `getAppleScriptState(): Promise<AppleScriptResult>` mit System-Events-Outer-Guard + Player-State-Authority + try/catch + TCC-Error-Detection.
- `npx tsc -p . --noEmit` ist clean.
- 5 atomic Commits im git log (1× npm install, 2× Type-Files, 1× lastfm, 1× applescript).
</success_criteria>

<output>
Nach Abschluss: erstelle `.planning/phases/02-now-playing-sources/02-01-SUMMARY.md` mit Frontmatter (requires/provides/affects, tech-stack, key-files, key-decisions, requirements-completed: SRC-01..05 partial, vollständige Erfüllung erst mit Plan 02-02), Performance-Block, Accomplishments-Liste, Task-Commits-Tabelle, Files-Created-Block, Decisions-Made (insbesondere die Output-Format-Konvention `STATE:`/`PLAY:` mit Tab-Separator, Wahl `runAppleScript` statt `execFile`, Sicherheits-Default `parsePlayerState → "paused"`), Deviations from Plan (falls vorhanden), Next-Phase-Readiness (Hinweis: Plan 02-02 importiert `getLastFmNowPlaying` und `getAppleScriptState`).
</output>
