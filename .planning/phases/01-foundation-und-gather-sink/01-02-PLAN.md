---
phase: 01-foundation-und-gather-sink
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/setup-ws.ts
  - src/sink/gather.ts
  - scripts/test-sink.ts
autonomous: false
requirements:
  - SINK-01
  - SINK-02
  - SINK-03
  - SINK-04
  - SINK-05
must_haves:
  truths:
    - "Polyfill `globalThis.WebSocket = WS` ist in `src/setup-ws.ts` als Side-Effect-Modul gesetzt"
    - "`src/sink/gather.ts` importiert `setup-ws.js` als ALLERERSTEN Import (vor `@gathertown/gather-game-client`)"
    - "GatherSink-Klasse exposed nur `connect()`, `setStatus(np)`, `clearStatus()`, `disconnect()` (plus `connected`-Flag)"
    - "`setStatus` ruft `setEmojiStatus` (♫) UND `setTextStatus` (Format `Artist – Track`) als zwei getrennte Actions"
    - "`clearStatus` leert beide (Emoji UND Text) auf leeren String"
    - "`tsc -p . --noEmit` ist clean"
    - "`scripts/test-sink.ts` connected, setzt hardcoded Track-Status, wartet 10s, leert Status, disconnected"
    - "Manuelle Verifikation: User sieht im Gather-Browser-Tab den Status `♫ Daft Punk – Around the World` für ~10s, dann leer"
  artifacts:
    - path: "src/setup-ws.ts"
      provides: "WebSocket-Polyfill als Side-Effect"
      contains: "globalThis.WebSocket"
    - path: "src/sink/gather.ts"
      provides: "GatherSink-Wrapper-Klasse"
      exports: ["GatherSink"]
    - path: "scripts/test-sink.ts"
      provides: "Smoke-Test gegen echtes Gather-Space"
      contains: "Around the World"
  key_links:
    - from: "src/sink/gather.ts"
      to: "src/setup-ws.js"
      via: "Top-of-file static import (Side-Effect)"
      pattern: "import \"[\\.\\/]+setup-ws"
    - from: "src/sink/gather.ts"
      to: "@gathertown/gather-game-client"
      via: "import { Game } NACH setup-ws-Import"
      pattern: "import .*Game.*gather-game-client"
    - from: "scripts/test-sink.ts"
      to: "src/sink/gather.js"
      via: "import { GatherSink }"
      pattern: "import .*GatherSink"
    - from: "GatherSink.setStatus"
      to: "game.sendAction setEmojiStatus + setTextStatus"
      via: "Zwei sendAction-Calls"
      pattern: "setEmojiStatus|setTextStatus"
---

<objective>
GatherSink-Wrapper anlegen: WebSocket-Polyfill als Side-Effect-Modul (`src/setup-ws.ts`), `GatherSink`-Klasse (`src/sink/gather.ts`) mit `connect`/`setStatus`/`clearStatus`/`disconnect` und einem `connected`-Flag, sowie Smoke-Test-Script (`scripts/test-sink.ts`), das die Bridge gegen den echten Gather-Space verifiziert (Status setzen, warten, leeren, disconnect).

Purpose: Phase 1 Success Criterion 1 ("Im Gather-Space erscheint nach Smoke-Test-Run das Emoji ♫ plus Statustext `Artist – Track`, beim Stoppen wird der Status geleert") wird hier erfüllt. Damit ist der Sink-Pfad Risiko-frei verifiziert, BEVOR Phase 2 (Sources) und Phase 3 (Loop) gegen ihn implementieren.

Output: Lauffähiger `npm run test:sink`-Befehl, der gegen das echte Gather-Space connected, ein hardcoded Daft-Punk-Beispiel als Status setzt, 10 Sekunden hält, leert, sauber disconnected. Manuelle visuelle Verifikation im Browser-Tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-und-gather-sink/01-CONTEXT.md
@.planning/phases/01-foundation-und-gather-sink/01-01-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@CLAUDE.md

<interfaces>
<!-- Aus Plan 01 verfügbare Module — Executor importiert direkt, keine Codebase-Exploration nötig: -->

Aus `src/types.ts`:
```typescript
export type NowPlaying = {
  artist: string;
  track: string;
} | null;
```

Aus `src/config.ts`:
```typescript
export type Config = {
  LASTFM_API_KEY: string;
  LASTFM_USER: string;
  GATHER_API_KEY: string;
  GATHER_SPACE_ID: string;
};
export const config: Config;  // top-level export, Validation läuft beim Import
```

Aus `src/logger.ts`:
```typescript
import type { Logger } from "pino";
export const log: Logger;  // pino-Instanz mit Redaction
```

Aus `@gathertown/gather-game-client@43.0.1` (offizielles Pattern aus STACK.md):
```typescript
import { Game } from "@gathertown/gather-game-client";
const game = new Game(SPACE_ID, () => Promise.resolve({ apiKey: API_KEY }));
game.connect();
game.subscribeToConnection((connected: boolean) => { /* ... */ });
game.sendAction({ $case: "setEmojiStatus", setEmojiStatus: { emojiStatus: "♫" } });
game.sendAction({ $case: "setTextStatus",  setTextStatus:  { textStatus:  "Artist – Track" } });
// disconnect(): siehe gather-game-client docs (game.disconnect() existiert)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `src/setup-ws.ts` — WebSocket-Polyfill als reines Side-Effect-Modul</name>
  <files>src/setup-ws.ts</files>
  <action>
Lege `src/setup-ws.ts` an. Inhalt:

```typescript
/**
 * WebSocket-Polyfill für `@gathertown/gather-game-client`.
 *
 * Pflicht (siehe SINK-02 + Pitfall 4):
 * Der Game-Client liest `globalThis.WebSocket` zur Modul-Load-Zeit. In
 * Node 22 existiert zwar `WebSocket` als globaler Symbol (seit Node 22.4),
 * aber der Game-Client prüft das nicht und nutzt das Symbol direkt — eine
 * unterschiedliche Implementierung (Browser-Spec vs. Undici-WebSocket-Spec)
 * würde u. U. nicht zu der erwarteten Form passen. Sicheres Pattern: explizit
 * mit `isomorphic-ws` (= `ws`-Polyfill für Node) überschreiben.
 *
 * Dieses Modul ist EIN reiner Side-Effect — KEIN Export. Wer es importiert,
 * triggert die Zuweisung. Der Import MUSS vor `@gathertown/gather-game-client`
 * stehen, in derselben Datei und in dieser Reihenfolge.
 *
 * Pattern aus dem offiziellen Gather-Reference-Repo `mod-spotify-as-status`.
 */
import WS from "isomorphic-ws";

(globalThis as { WebSocket?: unknown }).WebSocket = WS;
```

Wichtig:
- KEIN `export`, KEIN `default export` — das Modul wird nur als Side-Effect importiert (`import "./setup-ws.js";`).
- `(globalThis as { WebSocket?: unknown }).WebSocket = WS` — typsicheres Cast statt `as any`. Strict-Mode-friendly.
- KEIN Logging hier — der Logger könnte selbst auf Module-Loadtime-Reihenfolgen reagieren, defensiv halten.

ESM-Pflicht (Pitfall 21): Wer dieses Modul importiert, schreibt `import "./setup-ws.js"` (NICHT `./setup-ws` ohne Extension), damit `node dist/...` (Phase 4) das resolved.

Type-Check: `npx tsc -p . --noEmit` muss durchlaufen.
  </action>
  <verify>
    <automated>test -f src/setup-ws.ts && grep -q "globalThis" src/setup-ws.ts && grep -q "WebSocket" src/setup-ws.ts && grep -q "isomorphic-ws" src/setup-ws.ts && ! grep -q "^export" src/setup-ws.ts && npx tsc -p . --noEmit</automated>
  </verify>
  <done>`src/setup-ws.ts` existiert, setzt `globalThis.WebSocket` aus `isomorphic-ws`, hat KEINEN Export (reines Side-Effect-Modul). `tsc --noEmit` ohne Fehler.</done>
</task>

<task type="auto">
  <name>Task 2: `src/sink/gather.ts` — GatherSink-Klasse</name>
  <files>src/sink/gather.ts</files>
  <action>
Lege `src/sink/gather.ts` an. Inhalt:

```typescript
// WICHTIG: setup-ws MUSS der ERSTE Import sein, bevor `@gathertown/gather-game-client`
// geladen wird (siehe SINK-02 + Pitfall 4). Static-Imports werden in der
// Reihenfolge ihrer Deklaration evaluiert — diese Zeile triggert den Polyfill,
// die nächste Zeile lädt den Game-Client und liest dann das schon gesetzte
// `globalThis.WebSocket`.
import "../setup-ws.js";
import { Game } from "@gathertown/gather-game-client";

import type { NowPlaying } from "../types.js";
import { log } from "../logger.js";

/**
 * Wrapper um den Gather-Game-Client mit minimaler Public-API:
 * - `connect()` — verbindet zum Space, resolved wenn `connected === true` ist.
 * - `setStatus(np)` — setzt Emoji `♫` und Text `Artist – Track`.
 * - `clearStatus()` — leert beide Status (Pause/Stop).
 * - `disconnect()` — schließt die Verbindung sauber.
 * - `connected` (readonly) — aktueller Connection-State.
 *
 * Lifecycle-Annahmen für Phase 1 (siehe CONTEXT.md "Specifics"):
 * - `connect()` wartet auf das erste `subscribeToConnection(true)`-Event
 *   ODER timeoutet nach 10 s und wirft.
 * - KEIN automatischer Reconnect-Pfad in v1 (kommt in v2 / ROBUST-02).
 * - `setStatus`/`clearStatus` werfen NICHT bei `connected === false`,
 *   sondern loggen Warning — defensiv, weil der Caller nichts dagegen
 *   tun kann außer reconnecten.
 *
 * Status-Format (SINK-03): `♫` als Emoji, `${artist} – ${track}` als Text.
 * Der Gedankenstrich `–` (U+2013) ist hier Display-Format, nicht Prosa —
 * folgt dem Reference-Repo `mod-spotify-as-status`. Längen-Cap kommt in v2
 * (QOL-01, siehe Pitfall 16, akzeptiert für v1).
 */
export class GatherSink {
  private game: Game;
  private _connected = false;

  constructor(spaceId: string, apiKey: string) {
    this.game = new Game(spaceId, () => Promise.resolve({ apiKey }));
    this.game.subscribeToConnection((connected: boolean) => {
      this._connected = connected;
      log.info({ connected }, "[gather] connection state changed");
    });
  }

  get connected(): boolean {
    return this._connected;
  }

  async connect(timeoutMs = 10_000): Promise<void> {
    log.info("[gather] connecting...");
    this.game.connect();

    // Warte auf das erste connected=true-Event ODER Timeout.
    return new Promise<void>((resolve, reject) => {
      if (this._connected) {
        resolve();
        return;
      }
      const timer = setTimeout(() => {
        unsubscribe();
        reject(new Error(`Gather connect timeout after ${timeoutMs}ms`));
      }, timeoutMs);

      const unsubscribe = this.game.subscribeToConnection((connected: boolean) => {
        if (connected) {
          clearTimeout(timer);
          unsubscribe();
          resolve();
        }
      });
    });
  }

  setStatus(np: NonNullable<NowPlaying>): void {
    if (!this._connected) {
      log.warn({ np }, "[gather] setStatus called while disconnected, skipping");
      return;
    }
    const text = `${np.artist} – ${np.track}`;
    log.info({ text }, "[gather] setStatus");
    this.game.sendAction({
      $case: "setEmojiStatus",
      setEmojiStatus: { emojiStatus: "♫" },
    });
    this.game.sendAction({
      $case: "setTextStatus",
      setTextStatus: { textStatus: text },
    });
  }

  clearStatus(): void {
    if (!this._connected) {
      log.warn("[gather] clearStatus called while disconnected, skipping");
      return;
    }
    log.info("[gather] clearStatus");
    this.game.sendAction({
      $case: "setEmojiStatus",
      setEmojiStatus: { emojiStatus: "" },
    });
    this.game.sendAction({
      $case: "setTextStatus",
      setTextStatus: { textStatus: "" },
    });
  }

  disconnect(): void {
    log.info("[gather] disconnecting");
    // gather-game-client@43 exposed `disconnect()` auf der Game-Instanz.
    // Falls das in der konkreten 43.0.1-API anders heisst, hier anpassen
    // (mögliche Alternativen: `game.engine.disconnect()` oder
    // `game.unsubscribe()`).
    this.game.disconnect();
    this._connected = false;
  }
}
```

Wichtige Punkte:

1. **Import-Reihenfolge** ist Pflicht (Pitfall 4): `setup-ws.js` MUSS vor `@gathertown/gather-game-client` stehen. Kommentar im Code macht das explizit.
2. **`.js`-Extensions** in relativen Imports — siehe Pitfall 21 / `module: NodeNext`.
3. **Public-API entspricht SINK-05**: nur `connect`, `setStatus`, `clearStatus`, `disconnect`, plus `connected`-Getter. KEINE Game-Instanz-Leak.
4. **`setStatus` nimmt `NonNullable<NowPlaying>`** als Parameter — der Caller (Polling-Loop in Phase 3) muss vorher `null` filtern und entweder `setStatus` ODER `clearStatus` aufrufen. Das macht den Vertrag klarer als ein optionales `np?: NowPlaying`.
5. **Warning statt Throw bei `!connected`** — siehe Code-Kommentar. Defensives Verhalten: der Caller (Loop) soll nicht crashen, nur weil der Gather-Sink temporär down ist. Reconnect ist v2.
6. **`connect()` mit 10s-Timeout** — wenn das `connected=true`-Event nie kommt, wirft `connect()`. Der Caller (Smoke-Test, später Loop) entscheidet, was zu tun ist (für Phase 1 Smoke-Test: Crash mit Stack-Trace).
7. **`subscribeToConnection` zweimal** — einmal im Constructor (für `_connected`-State), einmal in `connect()` (für Promise-Resolve). Beide unsubscribed sauber.
8. **`subscribeToConnection` Rückgabewert ist eine Unsubscribe-Funktion** — Annahme aus typischen Pub-Sub-Pattern. Falls die 43.0.1-API kein Unsubscribe zurückgibt (sondern z. B. `void`), muss das hier angepasst werden. Bei der Implementierung im Zweifel via `node_modules/@gathertown/gather-game-client/dist/...` prüfen oder einen `disposed`-Boolean nutzen.

**Implementierungs-Note für den Executor:** Wenn `game.disconnect()` oder die `subscribeToConnection`-Unsubscribe-Signatur in der 43.0.1-API anders ist als im Code-Skelett, anpassen — Quelle ist `node_modules/@gathertown/gather-game-client/dist/...` oder die TypeDoc unter http://gather-game-client-docs.s3-website-us-west-2.amazonaws.com/ (38.x Linie, aber strukturell gleich). Den Status-Action-Pattern (`setEmojiStatus`/`setTextStatus`) NICHT ändern — der ist aus dem offiziellen Reference-Repo verifiziert.

Type-Check: `npx tsc -p . --noEmit` muss durchlaufen.
  </action>
  <verify>
    <automated>test -f src/sink/gather.ts && grep -q "import \"\\.\\./setup-ws" src/sink/gather.ts && grep -q "@gathertown/gather-game-client" src/sink/gather.ts && grep -q "setEmojiStatus" src/sink/gather.ts && grep -q "setTextStatus" src/sink/gather.ts && grep -q "export class GatherSink" src/sink/gather.ts && head -6 src/sink/gather.ts | grep -B1 "gather-game-client" | grep -q "setup-ws" && npx tsc -p . --noEmit</automated>
  </verify>
  <done>`src/sink/gather.ts` existiert, importiert `setup-ws.js` VOR `@gathertown/gather-game-client`, exportiert `GatherSink`-Klasse mit Public-API `connect`/`setStatus`/`clearStatus`/`disconnect` plus `connected`-Getter. Verwendet `setEmojiStatus` (♫) und `setTextStatus` (Format `Artist – Track`) via `game.sendAction`. `tsc --noEmit` ohne Fehler.</done>
</task>

<task type="auto">
  <name>Task 3: `scripts/test-sink.ts` — Smoke-Test mit hardcoded Track</name>
  <files>scripts/test-sink.ts</files>
  <action>
Lege `scripts/test-sink.ts` an. Inhalt:

```typescript
/**
 * Smoke-Test für GatherSink — gegen ECHTES Gather-Space.
 *
 * Voraussetzung: `.env` ist gefüllt mit echten Werten für GATHER_API_KEY und
 * GATHER_SPACE_ID. (LASTFM_*-Vars werden hier nicht genutzt, müssen aber
 * gesetzt sein, weil Config-Validation die ALLE checkt.)
 *
 * Ablauf:
 * 1. Connect zu Gather-Space.
 * 2. Setze Status `♫ Daft Punk – Around the World` (hardcoded).
 * 3. Warte 10 Sekunden — User verifiziert manuell im Gather-Browser-Tab.
 * 4. Leere Status (`clearStatus`).
 * 5. Warte 2 Sekunden — User verifiziert, dass Status weg ist.
 * 6. Disconnect, exit(0).
 *
 * Aufruf: `npm run test:sink`
 */
import { config } from "../src/config.js";
import { log } from "../src/logger.js";
import { GatherSink } from "../src/sink/gather.js";

async function main() {
  log.info("[test-sink] starting smoke test");

  const sink = new GatherSink(config.GATHER_SPACE_ID, config.GATHER_API_KEY);

  log.info("[test-sink] connecting...");
  await sink.connect();
  log.info({ connected: sink.connected }, "[test-sink] connected");

  log.info("[test-sink] setting status: Daft Punk – Around the World");
  sink.setStatus({ artist: "Daft Punk", track: "Around the World" });

  log.info("[test-sink] waiting 10 seconds — please verify status in Gather browser tab");
  await sleep(10_000);

  log.info("[test-sink] clearing status");
  sink.clearStatus();

  log.info("[test-sink] waiting 2 seconds — please verify status is empty");
  await sleep(2_000);

  log.info("[test-sink] disconnecting");
  sink.disconnect();

  log.info("[test-sink] smoke test complete");
  process.exit(0);
}

function sleep(ms: number): Promise<void> {
  return new Promise((resolve) => setTimeout(resolve, ms));
}

main().catch((err) => {
  log.fatal({ err }, "[test-sink] smoke test FAILED");
  process.exit(1);
});
```

Wichtige Punkte:

1. **Imports nutzen `.js`-Extensions** trotz `.ts`-Quellen — Pitfall 21 / NodeNext.
2. **`config`-Import triggert die Validation** beim ersten Import — wenn `.env` fehlt/unvollständig ist, exited der Prozess hier mit 0 (siehe `src/config.ts`). KEIN separater Check nötig.
3. **`process.exit(1)` im `.catch()`** — Smoke-Test ist KEIN Daemon, hier ist exit(1) bei Fehler korrekt (kein launchd-Loop-Risiko in Phase 1; das Phase-4-Constraint gilt für `src/index.ts`, nicht für `scripts/test-sink.ts`).
4. **`process.exit(0)` am Ende** — wichtig, weil `setTimeout` und WebSocket-Handles den Prozess sonst weiterleben lassen würden. Explizites Exit beendet Phase 1 sauber.
5. **Hardcoded Track aus CONTEXT.md** ("Daft Punk – Around the World") — nicht aus `.env` oder `process.argv`. Bewusst minimal: das Smoke-Test-Skript ist nur für diese eine Phase, nicht generisch.
6. **`sleep(10_000)`** — Standard-`Promise<setTimeout>`-Helper. Reicht für visuelle Verifikation.
7. **Logger-statt-`console.log`** — wir testen ja u. a. die Pino-Redaction. Wenn jemand z. B. `log.info({ config }, ...)` schreiben würde, müssten die API-Keys redactet sein. (Hier nicht explizit getestet; wir loggen nur sichere Felder.)

Type-Check: `npx tsc -p . --noEmit` muss durchlaufen (umfasst auch `scripts/**/*` per `tsconfig.json` `include`).
  </action>
  <verify>
    <automated>test -f scripts/test-sink.ts && grep -q "GatherSink" scripts/test-sink.ts && grep -q "Daft Punk" scripts/test-sink.ts && grep -q "Around the World" scripts/test-sink.ts && grep -q "sink.connect" scripts/test-sink.ts && grep -q "sink.setStatus" scripts/test-sink.ts && grep -q "sink.clearStatus" scripts/test-sink.ts && grep -q "sink.disconnect" scripts/test-sink.ts && npx tsc -p . --noEmit</automated>
  </verify>
  <done>`scripts/test-sink.ts` existiert, importiert `GatherSink`, `config`, `log`, ruft connect → setStatus(Daft Punk) → 10s wait → clearStatus → 2s wait → disconnect → exit(0). Fehler-Pfad mit exit(1). `tsc --noEmit` ohne Fehler.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manuelle Smoke-Test-Verifikation im Gather-Browser-Tab</name>
  <what-built>
Foundation (Plan 01) und GatherSink (Tasks 1–3 dieses Plans) sind komplett. Was Claude automatisch nicht testen kann: ob im Gather-Web-UI tatsächlich der Status `♫ Daft Punk – Around the World` erscheint und nach `clearStatus` wieder verschwindet. Das ist die einzige human-only Verifikation in Phase 1, weil Gather keine HTTP-API zum Auslesen des Player-Status anbietet (nur WebSocket-Receive über einen anderen Client).
  </what-built>
  <how-to-verify>
**Vorbereitung (User-Action, NICHT durch Claude):**

1. Falls noch nicht geschehen, `.env` aus `.env.example` kopieren und mit echten Werten füllen:
   ```
   LASTFM_API_KEY=<real>
   LASTFM_USER=<real>
   GATHER_API_KEY=<aus https://app.gather.town/apikeys>
   GATHER_SPACE_ID=<wxyz1234abcd/space-name aus der Gather-Space-URL>
   ```
2. Im Browser zum Gather-Space navigieren und einloggen (eigenen Avatar sehen).

**Smoke-Test-Run (User führt aus):**

```bash
npm run test:sink
```

**Erwartetes Verhalten:**

1. Terminal-Logs (JSON via Pino):
   - `[test-sink] starting smoke test`
   - `[gather] connecting...`
   - `[gather] connection state changed { connected: true }`
   - `[test-sink] connected`
   - `[gather] setStatus { text: "Daft Punk – Around the World" }`
   - `[test-sink] waiting 10 seconds...`
   - `[gather] clearStatus`
   - `[test-sink] waiting 2 seconds...`
   - `[gather] disconnecting`
   - `[test-sink] smoke test complete`

2. Im Browser-Tab (Gather-Space):
   - **Sekunde 0–10:** Eigener Avatar zeigt das Emoji `♫` und den Status-Text `Daft Punk – Around the World`.
   - **Sekunde 10–12:** Status ist leer (kein Emoji, kein Text mehr).
   - **Sekunde 12+:** Skript beendet, Status bleibt leer.

3. **Pino-Redaction-Spot-Check (manuell):** In den Logs darf NIRGENDS der echte `GATHER_API_KEY` oder `LASTFM_API_KEY` im Klartext stehen. Quick-Check:
   ```bash
   npm run test:sink 2>&1 | grep -iE 'api[-_]?key' | grep -v 'REDACTED'
   ```
   Output muss leer sein (keine unredacteten Keys).

**Wenn das funktioniert:** Phase 1 Success Criteria 1, 2, 4, 5 sind bestätigt (1 = Status setzen+leeren, 2 = `.env` lädt, 4 = Redaction aktiv, 5 = Polyfill funktioniert weil sonst `TypeError: WebSocket is not a constructor`).

**Wenn der Status NICHT erscheint:**
- Logs prüfen — wahrscheinlich falsche `GATHER_SPACE_ID` (Format `wxyz1234abcd/space-name`).
- Pitfall 4 prüfen: `import "../setup-ws.js"` MUSS die ERSTE Code-Zeile in `src/sink/gather.ts` sein.
- Pitfall 8: temporärer WebSocket-Trennfehler — einfach nochmal laufen lassen.

**Wenn `tsc --noEmit` aus früheren Tasks Fehler zeigt:**
- `disconnect()` existiert evtl. nicht auf der `Game`-Instanz in 43.0.1 — prüfe `node_modules/@gathertown/gather-game-client/dist/...` und ersetze ggf. mit der korrekten API.
  </how-to-verify>
  <resume-signal>Antworte mit "approved" wenn Status erscheint und wieder verschwindet, ODER beschreibe das Problem (z. B. "Status erscheint, verschwindet aber nicht" oder "TypeError: WebSocket is not a constructor").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `process.env` → Code → Gather-WebSocket | `GATHER_API_KEY` wird beim Connect über WSS an Gather geschickt. TLS schützt In-Transit; lokales Risiko = `.env` und Logs. |
| Code → `globalThis.WebSocket` | Polyfill ist global gesetzt — bei Mehrfach-Imports konsistent (idempotent: `WS` ist immer dieselbe Klasse). |
| `scripts/test-sink.ts` → Gather-Space | Hardcoded Test-Daten (Daft Punk), kein User-Input, keine externe URL-Manipulation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Gather-API-Key kompromittiert | accept | Plan-01-Threat T-01-06 deckt das ab (Single-User-Tool, lokales `.env`, kein Server). Pino-Redaction (Plan 01 Task 7) verhindert Leak in Logs. |
| T-02-02 | Tampering | WebSocket-Polyfill könnte überschrieben werden | accept | `globalThis.WebSocket` ist im Single-Process-Daemon idempotent — nur unser Code schreibt das. Kein User-Input, keine Plugins. |
| T-02-03 | Repudiation | Status-Setzungen sind nicht auditiert | accept | Single-User-Tool, kein Compliance-Bedarf. Pino-Logs reichen für Selbst-Debugging. |
| T-02-04 | Denial of Service | `connect()`-Timeout (10s) hängt Smoke-Test | mitigate | Timeout-Promise wirft, Smoke-Test exit(1) im `.catch()` — bricht statt hängt. |
| T-02-05 | Information Disclosure | `setTextStatus`-Inhalt geht über WSS an Gather, ist im Space für andere User sichtbar | accept | Per Design — das IST der Core-Value. Track-Namen sind nicht sensibel. Längen-Cap (Pitfall 16) v2-Thema. |
| T-02-06 | Elevation of Privilege | `gather-game-client` hat axios@0.26 + protobufjs CVEs (transitiv) | accept | Pitfall 18: Single-User-Tool, keine User-kontrollierten URLs/Bodies. Risiko praktisch null. README-Hinweis in Phase 4. |
</threat_model>

<verification>
1. `src/setup-ws.ts` existiert, setzt `globalThis.WebSocket` aus `isomorphic-ws`, hat keinen Export — Task 1 verify.
2. `src/sink/gather.ts` importiert `setup-ws.js` VOR `@gathertown/gather-game-client`, exportiert `GatherSink` mit Public-API `connect/setStatus/clearStatus/disconnect/connected` — Task 2 verify.
3. `scripts/test-sink.ts` ruft den vollständigen Connect → setStatus → wait → clearStatus → disconnect-Flow ab — Task 3 verify.
4. `npx tsc -p . --noEmit` ist clean (alle 3 Tasks enthalten den Check als Teil von `<automated>`).
5. **Manuelle Verifikation** (Task 4 Checkpoint):
   - Im Gather-Browser-Tab erscheint der Status `♫ Daft Punk – Around the World` für ~10s und verschwindet danach.
   - Logs enthalten keine unredacteten API-Keys (`grep -i 'api_key' | grep -v REDACTED` ist leer).
   - Kein `TypeError: WebSocket is not a constructor` (= Polyfill funktioniert).
</verification>

<success_criteria>
1. **`src/setup-ws.ts` existiert** als reines Side-Effect-Modul, setzt `globalThis.WebSocket` (SINK-02 erfüllt).
2. **`src/sink/gather.ts` importiert `setup-ws.js` als ersten Import**, exportiert `GatherSink`-Klasse (SINK-01, SINK-02, SINK-05 erfüllt).
3. **`setStatus(np)` ruft `setEmojiStatus` (♫) UND `setTextStatus` (`Artist – Track`)** als zwei separate `sendAction`-Calls (SINK-03 erfüllt).
4. **`clearStatus()` leert Emoji UND Text** auf leeren String (SINK-04 erfüllt).
5. **`scripts/test-sink.ts` führt den ganzen Lifecycle ab** und exited mit 0.
6. **`tsc --noEmit` ist clean.**
7. **Manuelle Verifikation im Gather-Tab erfolgreich** (Checkpoint Task 4): Status erscheint visuell, verschwindet nach `clearStatus`, keine API-Keys in Logs.
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/01-foundation-und-gather-sink/01-02-SUMMARY.md` mit:
- Liste der erstellten Dateien (`src/setup-ws.ts`, `src/sink/gather.ts`, `scripts/test-sink.ts`).
- Bestätigung, dass `tsc --noEmit` clean ist.
- Bestätigung, dass die manuelle Smoke-Test-Verifikation (Task 4) erfolgreich war (User hat den Status im Gather-Tab gesehen und das Verschwinden bestätigt).
- Beobachtungen aus dem Smoke-Test, die für Phase 2/3 relevant sein könnten:
  - Wie schnell kommt das `connected=true`-Event nach `connect()` (Latenz)?
  - Hat `subscribeToConnection` eine Unsubscribe-Signatur (oder musste die Impl angepasst werden)?
  - Heißt die Disconnect-Methode tatsächlich `game.disconnect()`, oder anders (für Phase 3 SIGTERM-Handler relevant)?
  - Sind `setEmojiStatus`/`setTextStatus` synchron-completing (sendAction wirft nicht, kein await nötig)?
- Coverage-Statement: SINK-01..05 sind in Plan 02 abgedeckt, alle 9 Phase-1-Requirements (CFG-01..04, SINK-01..05) sind erfüllt.
- Hinweis für Phase 2: Der `GatherSink` ist nutzbar, Sources werden den `setStatus`/`clearStatus`-Pfad nutzen.
</output>
</content>
