---
phase: 05-cdp-bridge-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/setup-ws.ts
  - src/sink/gather.ts
  - src/config.ts
  - src/index.ts
  - src/loop.ts
  - scripts/test-sink.ts
  - scripts/check-cdp.ts
  - .env.example
  - README.md
autonomous: false  # Task 9 ist optional checkpoint:human-verify
requirements: [SINK-01, SINK-02, SINK-03, SINK-04, SINK-05, CFG-01, CFG-02]

must_haves:
  truths:
    - "GatherSink schreibt Custom-Status (emoji 🎵 + text) via CDP gegen die lokal laufende GatherV2-Electron-App."
    - "GatherSink leert den Custom-Status via clearCustomStatus() über CDP."
    - "Beim Start prüft connect() ob der CDP-Port erreichbar ist und ob eine GatherV2-Page existiert; fehlt sie, wirft connect() mit klarer Setup-Anleitung."
    - "Config-Schema verlangt weder GATHER_API_KEY noch GATHER_SPACE_ID; optional liest es GATHER_CDP_PORT und GATHER_PAGE_URL_FILTER."
    - "@gathertown/gather-game-client, isomorphic-ws, ws sind vollständig aus dependencies entfernt; chrome-remote-interface@^0.33.3 ist installiert."
    - "src/setup-ws.ts existiert nicht mehr, kein Modul importiert es noch."
    - "tsc --noEmit ist clean nach jedem Code-Task und am Phasenende."
    - "scripts/test-sink.ts ruft `new GatherSink()` ohne Argumente und nutzt die async Public-API."
    - "src/loop.ts und src/index.ts awaiten alle Sink-Calls (setStatus/clearStatus); keine Fire-and-Forget-Promises."
    - "README dokumentiert den GatherV2-Start mit `--remote-debugging-port=9222`."
  artifacts:
    - path: "src/sink/gather.ts"
      provides: "CDP-basierte GatherSink mit emoji/text/clearCondition + clearCustomStatus"
      contains: "chrome-remote-interface"
    - path: "src/config.ts"
      provides: "Config-Schema ohne Gather-Pflichtkeys, optional CDP-Felder"
      contains: "GATHER_CDP_PORT"
    - path: "scripts/check-cdp.ts"
      provides: "CLI-Helper für Pre-Flight-Check des CDP-Ports und der GatherV2-Page"
    - path: "scripts/test-sink.ts"
      provides: "Smoke-Test gegen lokale GatherV2 via CDP"
      contains: "Daft Punk"
    - path: "package.json"
      provides: "chrome-remote-interface dep, keine gather-game-client/ws/isomorphic-ws mehr"
      contains: "chrome-remote-interface"
    - path: "README.md"
      provides: "GatherV2-Setup-Anleitung mit Debug-Flag"
      contains: "remote-debugging-port"
  key_links:
    - from: "src/sink/gather.ts"
      to: "chrome-remote-interface"
      via: "import CDP from 'chrome-remote-interface'"
      pattern: "from \"chrome-remote-interface\""
    - from: "src/sink/gather.ts"
      to: "http://localhost:PORT/json"
      via: "fetch in fetchTargets"
      pattern: "localhost:.*\\/json"
    - from: "src/loop.ts"
      to: "src/sink/gather.ts"
      via: "await sink.setStatus / await sink.clearStatus"
      pattern: "await sink\\.(set|clear)Status"
    - from: "src/index.ts"
      to: "src/sink/gather.ts"
      via: "await sink.clearStatus / await sink.disconnect im Shutdown"
      pattern: "await sink\\.(clearStatus|disconnect)"
---

<objective>
Bridge-Sink von Gather 1.0 (gather-game-client@43, WebSocket) auf Gather 2.0 (Chrome-DevTools-Protocol gegen lokale GatherV2-Electron-App) replatformen.

Purpose: Phase 1–4 nutzten den unmaintained `@gathertown/gather-game-client@43.0.1`, der seit 2023 nicht mehr released wurde und gegen `app.v2.gather.town` 404t. Spike vom 2026-05-08 hat verifiziert, dass GatherV2 als Electron-App mit `--remote-debugging-port=9222` CDP exposed und im Renderer `window.gatherDev.Repos.gameSpace.currentSpaceUser.setCustomStatus({emoji, text, clearCondition})` ruft. Wir tauschen die Sink-Implementierung aus, behalten die Public-API (`connect`, `setStatus`, `clearStatus`, `disconnect`, `connected`) bei und passen Config + Compat-Pfade in Loop/Index minimal an.

Output:
- `src/sink/gather.ts` neu (CDP-Pfad, gleiche Public-API, jetzt vollständig async)
- `src/setup-ws.ts` gelöscht (kein WS-Polyfill mehr nötig)
- `src/config.ts` ohne `GATHER_API_KEY`/`GATHER_SPACE_ID`-Pflicht, optional `GATHER_CDP_PORT`/`GATHER_PAGE_URL_FILTER`
- `package.json` ohne gather-game-client/isomorphic-ws/ws, mit `chrome-remote-interface@^0.33.3`
- `scripts/test-sink.ts` an die neue API adaptiert
- `scripts/check-cdp.ts` als CLI-Helper für Pre-Flight-Diagnose
- `README.md` mit GatherV2-Setup-Anleitung
- `src/loop.ts`/`src/index.ts` mit `await` an allen Sink-Calls
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cdp-bridge-refactor/05-CONTEXT.md
@.planning/phases/01-foundation-und-gather-sink/01-02-SUMMARY.md

<interfaces>
<!-- Aktuelle Signaturen, die nach diesem Plan gelten müssen. Direkt in CONTEXT.md verifiziert. -->
<!-- Executor: nutze diese Definitionen direkt, kein zusätzliches Codebase-Erkunden nötig. -->

GatherSink (NEU, src/sink/gather.ts):
```typescript
interface CDPConfig {
  port: number;          // default 9222
  pageUrlFilter: string; // default "app.v2.gather.town"
}

class GatherSink {
  constructor(cfg?: Partial<CDPConfig>);
  get connected(): boolean;
  connect(): Promise<void>;
  setStatus(np: NonNullable<NowPlaying>): Promise<void>;
  clearStatus(): Promise<void>;
  disconnect(): Promise<void>;
}
```

Config (NEU, src/config.ts):
```typescript
type Config = {
  LASTFM_API_KEY: string;        // optional, default ""
  LASTFM_USER: string;           // optional, default ""
  GATHER_CDP_PORT: string;       // optional, default "9222"
  GATHER_PAGE_URL_FILTER: string;// optional, default "app.v2.gather.town"
  // ENTFERNT: GATHER_API_KEY, GATHER_SPACE_ID
};
```

NowPlaying (UNVERÄNDERT, src/types.ts):
```typescript
type NowPlaying = { artist: string; track: string } | null;
```

CDP-Renderer-API (gefunden im Spike, NICHT von uns implementiert):
```javascript
window.gatherDev.Repos.gameSpace.currentSpaceUser.setCustomStatus({
  emoji?: string,
  text?: string,
  clearCondition: { type: "Never" } | { type: "DateTime", clearAt: Date }
});
window.gatherDev.Repos.gameSpace.currentSpaceUser.clearCustomStatus();
```
</interfaces>

<task_ordering_rationale>
Reihenfolge ist NICHT verhandelbar — die Phase enthält ein "Modul-Loch", solange der Sink im Umbau ist:

1. Erst Deps tauschen (`npm uninstall ... && npm install chrome-remote-interface`) — danach kompiliert der Code zwar nicht, aber die alte transitive WebSocket-Pflicht ist weg.
2. Erst dann `src/setup-ws.ts` löschen — die alte Sink importierte das Modul, also wäre `tsc` davor doppelt kaputt.
3. `src/sink/gather.ts` komplett ersetzen (kein `import "../setup-ws.js"` mehr, neuer CDP-Code).
4. `src/config.ts` aktualisieren — Felder weg, optionale CDP-Felder dazu.
5. `scripts/test-sink.ts` an neue Sink-Konstruktor-Signatur anpassen.
6. `src/index.ts` und `src/loop.ts` durchforsten und `await` an allen `setStatus`/`clearStatus`-Calls ergänzen, falls noch nicht da.
7. Erst hier muss `tsc --noEmit` clean sein. Vor Task 6 ist Code-Inkonsistenz erwartet.
8. Helper `scripts/check-cdp.ts` und README-Update — können nach dem Type-Check, weil sie keinen Code im Hot-Path beeinflussen.
9. Optionale human-verify (Smoke-Test gegen echte App) — User-Action.
</task_ordering_rationale>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependencies tauschen — gather-game-client raus, chrome-remote-interface rein</name>
  <files>package.json, package-lock.json</files>
  <action>
Im Projekt-Root:

```bash
npm uninstall @gathertown/gather-game-client isomorphic-ws ws @types/ws
npm install chrome-remote-interface@^0.33.3
npm install --save-dev @types/chrome-remote-interface
```

Hinweis zu `@types/chrome-remote-interface`: das Paket existiert nicht offiziell im DefinitelyTyped-Index für die aktuelle Major-Version 0.33; falls `npm install --save-dev @types/chrome-remote-interface` mit "404 Not Found" oder einem Lookup-Fehler bricht, das Kommando OHNE @types-Paket abschließen — `chrome-remote-interface` bringt eigene `*.d.ts` mit (verifiziere: `ls node_modules/chrome-remote-interface/*.d.ts`). Falls vorhanden, ist kein zusätzliches @types-Paket nötig. In dem Fall den `npm install --save-dev`-Aufruf nicht ausführen.

Nach dem Install verifizieren, dass `package.json` exakt diesen Zustand hat:
- `dependencies` enthält `chrome-remote-interface` (Version `^0.33.x`), KEIN `@gathertown/gather-game-client`, KEIN `isomorphic-ws`, KEIN `ws`.
- `devDependencies` enthält weiterhin `@types/node`, `tsx`, `typescript`. KEIN `@types/ws`.
- Alles andere (`dotenv`, `pino`, `run-applescript`, `zod`) bleibt unverändert.

KEIN `tsc --noEmit` jetzt — das wäre erwartet kaputt (alte Sink importiert noch entfernte Pakete). Type-Check kommt nach Task 6.

Atomic-Commit: `chore(05-01): swap gather-game-client+ws+isomorphic-ws for chrome-remote-interface`
  </action>
  <verify>
    <automated>grep -q '"chrome-remote-interface"' package.json &amp;&amp; ! grep -qE '"(@gathertown/gather-game-client|isomorphic-ws|^[[:space:]]*\"ws\")"' package.json &amp;&amp; test -d node_modules/chrome-remote-interface &amp;&amp; ! test -d node_modules/@gathertown</automated>
  </verify>
  <done>
- `package.json` `dependencies` enthält `chrome-remote-interface@^0.33.3`, weder `@gathertown/gather-game-client` noch `isomorphic-ws` noch `ws`.
- `node_modules/chrome-remote-interface/` existiert, `node_modules/@gathertown/` existiert nicht (mehr).
- `package-lock.json` ist konsistent (npm hat es geschrieben).
- Commit angelegt.
  </done>
</task>

<task type="auto">
  <name>Task 2: src/setup-ws.ts löschen</name>
  <files>src/setup-ws.ts</files>
  <action>
Die Datei ist nur noch Last: sie polyfillt `globalThis.WebSocket` für den alten gather-game-client, der jetzt weg ist. Ein expliziter `git rm` ist sauberer als `rm`, weil er den Stage-Eintrag direkt setzt:

```bash
git rm src/setup-ws.ts
```

Defensive-Check: Bevor du committest, suche im Repo nach Imports von `setup-ws`:

```bash
grep -rn "setup-ws" src/ scripts/ 2>/dev/null
```

Falls noch Treffer auftauchen (außer in `src/sink/gather.ts`, das Task 3 sowieso komplett überschreibt, und `src/index.ts`, das Task 6 anfasst), die Stelle notieren — sie muss in Task 3 oder Task 6 entfernt werden. Erwartet sind genau zwei Treffer: `src/index.ts` (`import "./setup-ws.js"`) und `src/sink/gather.ts` (`import "../setup-ws.js"`). Beide werden später gefixt.

Atomic-Commit: `refactor(05-01): remove src/setup-ws.ts (no WebSocket polyfill needed for CDP path)`
  </action>
  <verify>
    <automated>! test -e src/setup-ws.ts &amp;&amp; git diff --cached --name-status HEAD~0 2>/dev/null | grep -q "^D.*setup-ws.ts" || git log -1 --name-status | grep -q "setup-ws.ts"</automated>
  </verify>
  <done>
- `src/setup-ws.ts` existiert nicht mehr.
- Commit dokumentiert das Delete (`git log -1 --name-status` zeigt `D src/setup-ws.ts`).
- `grep -rn "setup-ws" src/ scripts/` zeigt höchstens noch zwei erwartete Treffer (`src/sink/gather.ts`, `src/index.ts`) — beide werden in Task 3 bzw. Task 6 entfernt.
  </done>
</task>

<task type="auto">
  <name>Task 3: src/sink/gather.ts komplett neu schreiben (CDP-Path)</name>
  <files>src/sink/gather.ts</files>
  <action>
Datei vollständig ersetzen mit der CDP-Implementierung aus 05-CONTEXT.md > Specifics. Wichtige Details (alle bewusst):

1. **Kein `import "../setup-ws.js"` mehr** — die Datei ist gelöscht.
2. **Default-Export ist die `GatherSink`-Klasse als Named-Export** (`export class GatherSink`). Public-API: `connect`, `setStatus`, `clearStatus`, `disconnect`, `connected` (getter).
3. **Konstruktor nimmt optionales `Partial<CDPConfig>`** — Aufruf ohne Args muss funktionieren (Smoke-Test in Task 5 ruft `new GatherSink()` ohne Parameter).
4. **`connect()` macht echten Pre-Flight**: ruft `fetch http://localhost:PORT/json`, sucht eine Page wo `t.type === "page" && t.url.includes(pageUrlFilter)`. Wenn nicht gefunden: throw mit der wörtlichen Fehlermeldung aus dem Skelett (`open -a GatherV2 --args --remote-debugging-port=...`). Setzt `_connected = true` bei Erfolg.
5. **`setStatus(np)` und `clearStatus()` werfen weiter im Fehlerfall** (Phase-3-Loop hat Try/Catch um jeden Tick — kein Crash). Aber sie geben klare Errors raus, wenn die Page weg ist.
6. **`runInPage(expression)` ist der Helper** (heisst absichtlich NICHT `eval`):
   - re-fetch Targets per Call (Page-WS-URL kann sich beim App-Restart ändern)
   - `CDP({ target: page.webSocketDebuggerUrl })` öffnen
   - `await Runtime.enable()` ZUERST
   - `await Runtime.evaluate({ expression: \`(async () => { ${expression}; })()\`, awaitPromise: true, returnByValue: true })`
   - bei `r.exceptionDetails`: throw mit der `exception.description` oder JSON-stringified Details
   - `try/finally`: `await client.close().catch(() => {})` — close-Errors schlucken (Idempotenz)
7. **`fetchTargets()` nutzt `AbortSignal.timeout(2000)`** — verhindert Hänger wenn Port offen aber nicht antwortet.
8. **JSON.stringify für eingefügte Werte** in `setStatus` — schützt gegen Anführungszeichen/Newlines im Track-Namen (z.B. `Don't Stop Me Now`).
9. **`disconnect()` ist no-op** — wir halten keine persistente Connection. Setzt nur `_connected = false`.
10. **Logger** weiter `pino` aus `../logger.js` mit `log.info`/`log.warn`/`log.error`.

Übernimm exakt das Skelett aus 05-CONTEXT.md > Specifics > `src/sink/gather.ts (neue Implementierung)`. Falls du Lücken siehst, hier die zwei einzigen erlaubten Anpassungen:

- **Optional defensiv**: in `connect()` zusätzlich `log.warn` (nicht `log.info`) auf den Fehlerpfad, BEVOR du wirfst — der Pino-Stream zeigt das auch wenn der Caller den Throw irgendwo schluckt.
- **Optional kompakt**: `runInPage` kann den `pageUrlFilter`-Match in eine private Methode auslagern, um Duplikation mit `connect()` zu vermeiden. Nicht zwingend, nur wenn du dabei keine Bugs einbaust.

Header-Kommentar (am Datei-Anfang) auf Deutsch, kurz, was das Modul macht und wie es sich von Phase 1 unterscheidet — verweis auf 05-CONTEXT.md für Detail-Begründung. Beispiel-Top-Kommentar:

```
/**
 * GatherSink (CDP-Pfad, Phase 5).
 *
 * Ersetzt die WebSocket-basierte Implementierung aus Phase 1, die gegen
 * Gather 2.0 (`app.v2.gather.town`) 404t. Statt direkt die WS-Protokolle
 * zu sprechen, ruft diese Implementierung die interne Mutation der lokal
 * laufenden GatherV2-Electron-App via Chrome-DevTools-Protocol auf.
 *
 * Public-API ist identisch zu Phase 1 — Caller (Loop, Index, Smoke-Test)
 * müssen nur `await` an `setStatus`/`clearStatus` ergänzen (waren vorher sync).
 *
 * Voraussetzung: GatherV2 läuft mit `--remote-debugging-port=9222`.
 * Siehe README + .planning/phases/05-cdp-bridge-refactor/05-CONTEXT.md.
 */
```

Nach dem Schreiben:

```bash
npx tsc -p . --noEmit
```

erwartet ist: noch RED, weil `scripts/test-sink.ts` (Task 5) und `src/index.ts` (Task 6) noch die alte Konstruktor-Signatur `new GatherSink(spaceId, apiKey)` rufen. **Das ist OK** — Type-Check-Gate ist nach Task 6.

Atomic-Commit: `feat(05-01): rewrite GatherSink to use Chrome-DevTools-Protocol against GatherV2 Electron app`
  </action>
  <verify>
    <automated>grep -q "import CDP from \"chrome-remote-interface\"" src/sink/gather.ts &amp;&amp; grep -q "setCustomStatus" src/sink/gather.ts &amp;&amp; grep -q "clearCustomStatus" src/sink/gather.ts &amp;&amp; grep -q "runInPage" src/sink/gather.ts &amp;&amp; ! grep -q "setup-ws" src/sink/gather.ts &amp;&amp; ! grep -q "@gathertown/gather-game-client" src/sink/gather.ts &amp;&amp; grep -q "AbortSignal.timeout" src/sink/gather.ts &amp;&amp; grep -cE "JSON\\.stringify" src/sink/gather.ts | awk '$1>=2{exit 0}{exit 1}'</automated>
  </verify>
  <done>
- `src/sink/gather.ts` importiert `chrome-remote-interface`, KEIN `setup-ws`, KEIN `@gathertown/gather-game-client`.
- `setCustomStatus`-Aufruf mit `emoji`, `text`, `clearCondition: {type: "Never"}` ist im Code.
- `clearCustomStatus()`-Aufruf ist im Code.
- `runInPage(expression)`-Methode existiert (Name absichtlich nicht `eval`).
- `AbortSignal.timeout(2000)` schützt den `/json`-Fetch.
- JSON.stringify wird für eingefügte Werte (mind. 2 Vorkommen: emoji + text) genutzt.
- `try/finally` mit `client.close()` im `runInPage`.
- Public-API: `constructor(cfg?: Partial<CDPConfig>)`, `connect()`, `setStatus(np)`, `clearStatus()`, `disconnect()`, `connected` getter — alle vier Methoden Promise-returning.
- Commit angelegt.
  </done>
</task>

<task type="auto">
  <name>Task 4: src/config.ts — Gather-Pflichtkeys raus, optionale CDP-Felder rein</name>
  <files>src/config.ts, .env.example</files>
  <action>
Im `src/config.ts`:

1. **Aus dem Zod-Schema entfernen**: `GATHER_API_KEY`, `GATHER_SPACE_ID`. Beide Zeilen gelöscht.
2. **Hinzufügen** (beide optional mit Default):
   ```ts
   GATHER_CDP_PORT: z.string().optional().default("9222"),
   GATHER_PAGE_URL_FILTER: z.string().optional().default("app.v2.gather.town"),
   ```
3. **Header-Kommentar zum Schema aktualisieren** — der bisherige Text sagt "Gather-Keys sind Pflicht (Sink ohne sie sinnlos)". Ersetzen durch eine Zeile, die erklärt, dass die Bridge ab Phase 5 gegen die lokale GatherV2-App via CDP arbeitet und keine API-Keys mehr braucht (LASTFM-Vars bleiben optional, CDP-Vars haben Defaults).
4. **`Config`-Type bleibt `z.infer<typeof EnvSchema>`** — der Type wird automatisch korrekt.
5. **`config`-Export bleibt `loadConfig()`-Result** — keine Signatur-Änderung.
6. Die `.refine`-Klausel für die LASTFM-Konsistenz BLEIBT unverändert (Phase 2 hängt daran).
7. **Defensive Type-Annotation prüfen**: an keiner Stelle in `config.ts` wird `process.env.GATHER_API_KEY` oder `process.env.GATHER_SPACE_ID` direkt gelesen.

Im `.env.example`:

1. Die zwei Zeilen `GATHER_API_KEY=` und `GATHER_SPACE_ID=` entfernen.
2. Zwei optionale Zeilen hinzufügen, jeweils mit `#`-Kommentar:
   ```
   # Optional: Port, auf dem die GatherV2-Electron-App ihren Chrome-DevTools-Protocol-Server hört.
   # Default 9222. Muss zum Flag passen, mit dem die App gestartet wurde.
   #GATHER_CDP_PORT=9222

   # Optional: URL-Substring, anhand dessen die GatherV2-Page im CDP gefunden wird.
   # Default "app.v2.gather.town".
   #GATHER_PAGE_URL_FILTER=app.v2.gather.town
   ```
   Beide Zeilen sind auskommentiert (`#`-Prefix) — das Tool läuft mit den Defaults, ohne dass irgendetwas in `.env` gesetzt sein muss.

Den LASTFM-Block in `.env.example` unverändert lassen.

Nach dem Schreiben:

```bash
npx tsc -p . --noEmit
```

erwartet ist immer noch RED — `scripts/test-sink.ts` (Task 5) liest noch `config.GATHER_SPACE_ID` und `config.GATHER_API_KEY`. Type-Check-Gate ist nach Task 6.

Atomic-Commit: `feat(05-01): drop GATHER_API_KEY/GATHER_SPACE_ID from config, add optional CDP_PORT and PAGE_URL_FILTER`
  </action>
  <verify>
    <automated>! grep -q "GATHER_API_KEY" src/config.ts &amp;&amp; ! grep -q "GATHER_SPACE_ID" src/config.ts &amp;&amp; grep -q "GATHER_CDP_PORT" src/config.ts &amp;&amp; grep -q "GATHER_PAGE_URL_FILTER" src/config.ts &amp;&amp; grep -q "9222" src/config.ts &amp;&amp; grep -q "app.v2.gather.town" src/config.ts &amp;&amp; ! grep -E "^GATHER_API_KEY|^GATHER_SPACE_ID" .env.example</automated>
  </verify>
  <done>
- `src/config.ts` enthält weder `GATHER_API_KEY` noch `GATHER_SPACE_ID`.
- Schema enthält `GATHER_CDP_PORT` (default `"9222"`) und `GATHER_PAGE_URL_FILTER` (default `"app.v2.gather.town"`).
- `.env.example` enthält keine unkommentierten Zeilen für `GATHER_API_KEY` oder `GATHER_SPACE_ID`.
- LASTFM-Konsistenz-Refine ist erhalten.
- Commit angelegt.
  </done>
</task>

<task type="auto">
  <name>Task 5: scripts/test-sink.ts an neue Sink-API anpassen</name>
  <files>scripts/test-sink.ts</files>
  <action>
Die Datei nutzt aktuell `new GatherSink(config.GATHER_SPACE_ID, config.GATHER_API_KEY)` und ruft `setStatus`/`clearStatus` synchron. Beides muss weg.

Neuer Ablauf (alle Schritte awaiten):

```typescript
import { log } from "../src/logger.js";
import { GatherSink } from "../src/sink/gather.js";

async function main() {
  log.info("[test-sink] starting smoke test");

  // Kein Argument — Defaults aus CDPConfig (port 9222, filter app.v2.gather.town).
  // Falls Port/Filter via .env überschrieben werden sollen, manuell aus
  // `config` ziehen — für den Smoke-Test reichen die Defaults.
  const sink = new GatherSink();

  log.info("[test-sink] pre-flight CDP check...");
  await sink.connect();
  log.info({ connected: sink.connected }, "[test-sink] CDP page found, ready");

  log.info("[test-sink] setting status: 🎵 Daft Punk – Around the World");
  await sink.setStatus({ artist: "Daft Punk", track: "Around the World" });

  log.info("[test-sink] waiting 10 seconds — please verify status in GatherV2 UI");
  await sleep(10_000);

  log.info("[test-sink] clearing status");
  await sink.clearStatus();

  log.info("[test-sink] waiting 2 seconds — please verify status is empty");
  await sleep(2_000);

  log.info("[test-sink] disconnecting");
  await sink.disconnect();

  log.info("[test-sink] smoke test complete");
  process.exit(0);
}

function sleep(ms: number): Promise<void> {
  return new Promise((resolve) => setTimeout(resolve, ms));
}

main().catch((err) => {
  log.fatal({ err }, "[test-sink] smoke test FAILED");
  process.exit(1);
});
```

Wichtige Details:
- KEIN `import { config }` mehr — der Smoke-Test braucht weder `GATHER_API_KEY` noch `GATHER_SPACE_ID`. Damit fällt auch der Pflicht-Check der LASTFM-Konsistenz weg, der durch den `import "../src/config.js"` getriggert wurde — das ist OK, wir wollen den Smoke-Test gegen GatherV2 unabhängig von der LASTFM-Config laufen lassen.
- **Ausnahme**: Falls der ESLint-Setup oder der Type-Checker den `import` von `config` weiterhin erwartet (er tut das nicht, das war nur für die alten Konstruktor-Args nötig) — kein Workaround einbauen, einfach weglassen.
- Header-Kommentar (DocBlock am Datei-Anfang) komplett ersetzen. Beispiel:
  ```
  /**
   * Smoke-Test für GatherSink (Phase 5, CDP-Pfad).
   *
   * Voraussetzung: GatherV2-Electron-App läuft mit
   *   open -a GatherV2 --args --remote-debugging-port=9222
   * und du bist im Space eingeloggt (eigener Avatar im UI sichtbar).
   *
   * Ablauf:
   * 1. Pre-Flight (`sink.connect()`) — sucht GatherV2-Page auf localhost:9222.
   * 2. setStatus(`{artist:"Daft Punk", track:"Around the World"}`) — zeigt 🎵 + Text.
   * 3. 10s warten — User verifiziert visuell im GatherV2-UI.
   * 4. clearStatus() — UI-Status verschwindet.
   * 5. 2s warten — User verifiziert leeren Status.
   * 6. disconnect() (no-op, weil per-call CDP), exit(0).
   *
   * Aufruf: `npm run test:sink`
   */
  ```

Nach dem Schreiben:

```bash
npx tsc -p . --noEmit
```

erwartet: weiter RED, weil `src/index.ts` noch `new GatherSink(config.GATHER_SPACE_ID, config.GATHER_API_KEY)` ruft. Type-Check-Gate ist nach Task 6.

Atomic-Commit: `refactor(05-01): adapt scripts/test-sink.ts to async CDP GatherSink API (no env-args)`
  </action>
  <verify>
    <automated>grep -q "new GatherSink()" scripts/test-sink.ts &amp;&amp; grep -q "await sink.setStatus" scripts/test-sink.ts &amp;&amp; grep -q "await sink.clearStatus" scripts/test-sink.ts &amp;&amp; grep -q "await sink.connect" scripts/test-sink.ts &amp;&amp; grep -q "await sink.disconnect" scripts/test-sink.ts &amp;&amp; ! grep -q "GATHER_API_KEY" scripts/test-sink.ts &amp;&amp; ! grep -q "GATHER_SPACE_ID" scripts/test-sink.ts &amp;&amp; grep -q "Daft Punk" scripts/test-sink.ts &amp;&amp; grep -q "Around the World" scripts/test-sink.ts</automated>
  </verify>
  <done>
- `scripts/test-sink.ts` ruft `new GatherSink()` ohne Argumente.
- Alle vier Sink-Methoden (`connect`, `setStatus`, `clearStatus`, `disconnect`) werden ge-`await`-et.
- KEIN `GATHER_API_KEY`/`GATHER_SPACE_ID`-Lookup mehr in der Datei.
- Hardcoded-Test-Track (`Daft Punk` / `Around the World`) ist drin.
- Commit angelegt.
  </done>
</task>

<task type="auto">
  <name>Task 6: src/index.ts und src/loop.ts auf await-Compat aktualisieren, finalen tsc-Clean prüfen</name>
  <files>src/index.ts, src/loop.ts</files>
  <action>
Beide Dateien durchforsten und alle Sink-Calls awaiten. Stand vor diesem Task (verifiziert):

**`src/loop.ts`** Zeilen 46–48 enthalten:
```ts
if (np === null) {
  sink.clearStatus();   // synchron, muss await werden
} else {
  sink.setStatus(np);   // synchron, muss await werden
}
```
Das ist innerhalb der `tick`-Funktion, die schon `async` ist — also einfach `await` davorsetzen:
```ts
if (np === null) {
  await sink.clearStatus();
} else {
  await sink.setStatus(np);
}
```

Der DocBlock von `runLoop` referenziert nicht direkt die Synchronizität dieser Calls — keine Kommentar-Anpassung nötig. Falls dir Inkonsistenzen im Kommentar auffallen (z.B. eine Aussage wie "synchroner sendAction"), korrigiere sie minimal.

**`src/index.ts`** hat zwei betroffene Stellen:

1. **Zeile 1**: `import "./setup-ws.js";` — komplett entfernen (Datei ist gelöscht). Den Kommentarblock direkt darunter (Zeilen 2–5) ebenfalls entfernen, weil er den nicht mehr existierenden Polyfill erklärt.

2. **Zeile 56**: `sink.clearStatus();` im Shutdown-Handler:
   ```ts
   try {
     sink.clearStatus();        // muss await werden
   } catch (err) {
     log.warn({ err }, "[shutdown] clearStatus threw");
   }
   await sink.disconnect();
   ```
   Wird zu:
   ```ts
   try {
     await sink.clearStatus();
   } catch (err) {
     log.warn({ err }, "[shutdown] clearStatus threw");
   }
   await sink.disconnect();
   ```

3. **Zeile 82**: Konstruktor-Aufruf:
   ```ts
   const sink = new GatherSink(config.GATHER_SPACE_ID, config.GATHER_API_KEY);
   ```
   Wird zu:
   ```ts
   const sink = new GatherSink({
     port: Number(config.GATHER_CDP_PORT),
     pageUrlFilter: config.GATHER_PAGE_URL_FILTER,
   });
   ```
   `config.GATHER_CDP_PORT` ist `string` (Zod default `"9222"`), `Number()` konvertiert es. Der CDPConfig-Port ist `number`.

4. Pino-Redaction: Falls `src/logger.ts` aktuell `env.GATHER_API_KEY` in der `redact`-Liste hat, dort ggf. anpassen. Beispiel-Read:
   ```bash
   grep -n "GATHER_API_KEY\|GATHER_SPACE_ID" src/logger.ts 2>/dev/null
   ```
   Falls Treffer: die Pfade aus dem `redact`-Array entfernen. Falls keine Treffer: nichts tun, `src/logger.ts` bleibt unverändert (logger.ts wurde im CONTEXT als unverändert markiert).

Nach allen drei Änderungen:

```bash
npx tsc -p . --noEmit
```

**MUSS jetzt clean sein** — das ist das harte Type-Check-Gate dieser Phase. Wenn er nicht clean ist, lies die Fehler genau:
- "Property X does not exist on Config" → Task 4 hat Felder vergessen oder Task 6 referenziert ein Feld, das es nicht gibt.
- "Expected 0 arguments, got 2" auf `new GatherSink` → irgendwo wird der alte Konstruktor noch gerufen (grep nach `new GatherSink(` und prüfe alle Treffer).
- "Property setStatus does not exist on type" → Sink-Pfad/Import broken.

Erst nach `tsc --noEmit` clean committen.

Atomic-Commit: `refactor(05-01): wire CDP GatherSink into index.ts and loop.ts (await async sink calls, drop setup-ws import)`
  </action>
  <verify>
    <automated>npx tsc -p . --noEmit &amp;&amp; ! grep -q "setup-ws" src/index.ts &amp;&amp; ! grep -q "GATHER_API_KEY\|GATHER_SPACE_ID" src/index.ts &amp;&amp; grep -q "await sink.clearStatus" src/index.ts &amp;&amp; grep -q "await sink.clearStatus" src/loop.ts &amp;&amp; grep -q "await sink.setStatus" src/loop.ts &amp;&amp; grep -q "GATHER_CDP_PORT" src/index.ts</automated>
  </verify>
  <done>
- `npx tsc -p . --noEmit` läuft mit Exit-Code 0 (CLEAN).
- `src/index.ts` importiert `setup-ws.js` nicht mehr und ruft `new GatherSink({port, pageUrlFilter})`.
- `src/index.ts` Shutdown-Handler awaitet `sink.clearStatus()`.
- `src/loop.ts` awaitet sowohl `sink.setStatus(np)` als auch `sink.clearStatus()`.
- KEIN Treffer für `GATHER_API_KEY` oder `GATHER_SPACE_ID` mehr im gesamten `src/` und `scripts/`-Tree (außer `.env.example`-Kommentare gibt es eh nicht mehr).
- Commit angelegt.
  </done>
</task>

<task type="auto">
  <name>Task 7: scripts/check-cdp.ts als Pre-Flight-Helper anlegen</name>
  <files>scripts/check-cdp.ts, package.json</files>
  <action>
Neue Datei `scripts/check-cdp.ts` mit dem Skelett aus 05-CONTEXT.md > Specifics > Optional Helper. Ziel: User kann `npm run check-cdp` rufen, um zu prüfen ob die GatherV2-App korrekt mit Debug-Flag läuft, OHNE den vollen Smoke-Test (mit Status-Setzen) zu starten.

Code (nahe am Skelett, mit minimalen Härtungen):

```typescript
/**
 * CDP-Pre-Flight-Check für die GatherV2-Electron-App.
 *
 * Läuft eigenständig, ohne `dotenv`/`config`-Import — liest die zwei optionalen
 * Env-Vars direkt, fällt auf Defaults zurück. So kann der User auch debuggen,
 * wenn die `.env` kaputt ist.
 *
 * Aufruf: `npm run check-cdp`
 */

const port = process.env.GATHER_CDP_PORT ?? "9222";
const filter = process.env.GATHER_PAGE_URL_FILTER ?? "app.v2.gather.town";

try {
  const r = await fetch(`http://localhost:${port}/json`, {
    signal: AbortSignal.timeout(2000),
  });
  if (!r.ok) throw new Error(`CDP HTTP ${r.status}`);
  const targets = (await r.json()) as Array<{ type: string; url: string; webSocketDebuggerUrl?: string }>;
  const page = targets.find(
    (t) => t.type === "page" && t.url.includes(filter),
  );
  if (!page) {
    console.error(
      `❌ CDP läuft auf localhost:${port}, aber keine Page mit URL-Substring "${filter}" gefunden.`,
    );
    console.error(
      `   Vermutlich: GatherV2 läuft, aber du bist nicht im Space eingeloggt, oder die App ist auf der Login-Page.`,
    );
    console.error(`   Aktuelle Pages:`);
    for (const t of targets) {
      console.error(`     [${t.type}] ${t.url}`);
    }
    process.exit(1);
  }
  console.log(`✅ GatherV2-Page erreichbar: ${page.url}`);
  console.log(`   WebSocket: ${page.webSocketDebuggerUrl ?? "(kein webSocketDebuggerUrl gemeldet)"}`);
  process.exit(0);
} catch (e: unknown) {
  const msg = e instanceof Error ? e.message : String(e);
  console.error(`❌ CDP nicht erreichbar auf localhost:${port}.`);
  console.error(`   Starte GatherV2 mit:`);
  console.error(`     open -a GatherV2 --args --remote-debugging-port=${port}`);
  console.error(`   Detail: ${msg}`);
  process.exit(1);
}
```

Hinweise zur Umsetzung:
- **Top-Level-await ist OK** — `tsconfig.json` ist bereits `module: NodeNext` mit `target: ES2022+`, und das Projekt ist ESM (`"type": "module"`). Falls `tsc --noEmit` doch über Top-Level-await meckert, in eine `async function main()` plus `main()` am Ende wrappen.
- **`.js`-Extensions in Imports**: dieser Helper hat KEINE projektinternen Imports — der Pfad ist sauber.
- **Kein Logger** — `console.log/error` reichen für ein CLI-Tool, dessen Output direkt im Terminal landet (nicht launchd-routed).

In `package.json` einen npm-Script ergänzen:
```json
"check-cdp": "tsx scripts/check-cdp.ts",
```
Position: zwischen `test:sink` und `test:sources` oder am Ende der `scripts`-Liste — Reihenfolge ist semantisch egal.

Type-Check:
```bash
npx tsc -p . --noEmit
```
muss weiter clean sein.

Atomic-Commit: `feat(05-01): add scripts/check-cdp.ts CLI helper for pre-flight CDP/GatherV2-page diagnostics`
  </action>
  <verify>
    <automated>npx tsc -p . --noEmit &amp;&amp; test -f scripts/check-cdp.ts &amp;&amp; grep -q "check-cdp" package.json &amp;&amp; grep -q "AbortSignal.timeout" scripts/check-cdp.ts &amp;&amp; grep -q "remote-debugging-port" scripts/check-cdp.ts</automated>
  </verify>
  <done>
- `scripts/check-cdp.ts` existiert, prüft `localhost:PORT/json`, sucht Page nach Filter, druckt klare Fehler-Output bei beiden Failure-Modes (CDP unreachable / page missing).
- `package.json` hat `"check-cdp": "tsx scripts/check-cdp.ts"` in `scripts`.
- `npx tsc -p . --noEmit` weiter clean.
- Commit angelegt.
  </done>
</task>

<task type="auto">
  <name>Task 8: README.md auf GatherV2-CDP-Setup aktualisieren</name>
  <files>README.md</files>
  <action>
Das aktuelle README erklärt Phase-1-bis-4-Setup mit `GATHER_API_KEY` und `GATHER_SPACE_ID`. Nach Phase 5 ist beides veraltet. Update-Strategie: kein vollständiger Rewrite, sondern gezielte Edits.

Ziele:

1. **Voraussetzungen** (Sektion `## Voraussetzungen`): die zwei Punkte "Last.fm-Account mit API-Key" und "Gather-Space mit API-Key" anpassen:
   - Last.fm-Punkt darf bleiben, mit Hinweis "optional, ohne Last.fm fällt der Daemon auf AppleScript-only zurück".
   - "Gather-Space mit API-Key" durch ersetzen: **"GatherV2-Desktop-App (`app.v2.gather.town`) installiert, Account-Login funktioniert"**.

2. **Setup** (Sektion `## Setup`): den Schritt 2 (`.env` mit allen vier Keys füllen) ersetzen durch:
   - `cp .env.example .env`
   - "Optional: Last.fm-Vars eintragen, falls Last.fm als Now-Playing-Source genutzt werden soll. Ohne sie läuft der Daemon AppleScript-only."
   - **Kein Wort mehr von `GATHER_API_KEY` oder `GATHER_SPACE_ID`** — beide existieren nicht mehr.

3. **Neuer Abschnitt** direkt nach `## Setup`, vor `## Daemon-Steuerung`:

   ```markdown
   ## GatherV2 mit Debug-Flag starten

   Die Bridge spricht ab Phase 5 nicht mehr das Gather-1.0-WebSocket-Protokoll, sondern steuert die lokal laufende GatherV2-Electron-App via Chrome-DevTools-Protocol (CDP). Voraussetzung dafür: die App muss mit dem Debug-Port-Flag gestartet sein.

   **Einmalige Test-Session:**

   ```bash
   open -a GatherV2 --args --remote-debugging-port=9222
   ```

   Login im Space, dann verifizieren:

   ```bash
   npm run check-cdp
   ```

   Erwartete Ausgabe: `✅ GatherV2-Page erreichbar: https://app.v2.gather.town/...`.

   **Persistente Lösung (empfohlen):** GatherV2 als Login-Item mit dem Flag eintragen. Aktuell out-of-scope der v1, manuell oder via separater Phase v2.1.

   **Wenn der Flag fehlt:** Der Daemon loggt beim ersten Tick `[gather] no GatherV2 page found at localhost:9222` und überspringt den Tick. Beim nächsten Tick (10s später) versucht er es erneut. Kein Crash.
   ```

4. **Architektur** (Sektion `## Architektur`): die Bullet-Liste anpassen:
   - "WebSocket-Polyfill" raus.
   - `src/sink/`-Beschreibung ersetzen durch: **"`src/sink/` enthält den CDP-Wrapper gegen die GatherV2-Electron-App (`setCustomStatus`/`clearCustomStatus` via Chrome-DevTools-Protocol)"**.
   - `src/index.ts` Beschreibung: "WebSocket-Polyfill" durch nichts ersetzen (einfach den Begriff streichen, der Rest stimmt).

5. **Troubleshooting**: einen neuen Sub-Abschnitt anhängen direkt VOR `### Audit-Warnungen`:

   ```markdown
   ### CDP nicht erreichbar / GatherV2-Page nicht gefunden

   1. `npm run check-cdp` ausführen — der Helper sagt dir präzise, welcher der zwei Failure-Modes vorliegt:
      - **CDP-Port `localhost:9222` antwortet nicht** → GatherV2 läuft nicht oder wurde ohne `--remote-debugging-port`-Flag gestartet. Lösung:
        ```bash
        # GatherV2 sauber beenden, dann:
        open -a GatherV2 --args --remote-debugging-port=9222
        ```
      - **Port antwortet, aber keine `app.v2.gather.town`-Page** → App ist auf Login-Page oder du bist ausgeloggt. Im UI einloggen und in den Space gehen.

   2. Falls du einen anderen Port nutzen willst (z.B. wenn 9222 belegt ist):
      ```bash
      open -a GatherV2 --args --remote-debugging-port=9333
      # in der .env:
      GATHER_CDP_PORT=9333
      ```
   ```

6. **Audit-Warnings** (`### Audit-Warnungen`): den Absatz komplett ersetzen durch:

   ```markdown
   ### Audit-Warnungen (`npm audit`)

   Vor Phase 5 zog `@gathertown/gather-game-client` alte axios- und protobufjs-Versionen mit CVE-Findings ein. Mit dem CDP-Refactor ist diese Dependency raus, `npm audit` sollte deutlich ruhiger sein. `chrome-remote-interface` ist gut gepflegt; falls dort eigene Audit-Hits auftauchen, lokal prüfen — das Tool ist Single-User und kontaktiert keine User-kontrollierten URLs.
   ```

Nach den Edits den Inhalt einmal in der Vorschau überfliegen und sicherstellen, dass es keine widersprüchlichen Aussagen mehr gibt (z.B. "vier Keys eintragen" irgendwo übrig). Falls ja: streichen.

Atomic-Commit: `docs(05-01): update README for GatherV2 CDP path (debug-port flag, check-cdp helper, troubleshooting)`
  </action>
  <verify>
    <automated>! grep -q "GATHER_API_KEY\|GATHER_SPACE_ID" README.md &amp;&amp; grep -q "remote-debugging-port" README.md &amp;&amp; grep -q "check-cdp" README.md &amp;&amp; grep -q "Chrome-DevTools-Protocol\|CDP" README.md &amp;&amp; grep -q "GatherV2" README.md</automated>
  </verify>
  <done>
- README erwähnt nirgends mehr `GATHER_API_KEY` oder `GATHER_SPACE_ID`.
- Setup-Sektion erklärt GatherV2-Start mit `--remote-debugging-port`.
- Neue Troubleshooting-Sektion für CDP unreachable / page missing.
- Architektur-Bullet erwähnt `setCustomStatus` und CDP statt WebSocket-Polyfill.
- `npm run check-cdp` ist als Diagnose-Befehl referenziert.
- Commit angelegt.
  </done>
</task>

<task type="checkpoint:human-verify" gate="optional">
  <name>Task 9: Optionale visuelle Verifikation gegen echte GatherV2-App</name>
  <what-built>
- Komplette CDP-Bridge: GatherSink + Pre-Flight-Helper + adaptierter Smoke-Test + README-Update.
- `npx tsc -p . --noEmit` clean.
- Alle Tasks committet, atomic.
  </what-built>
  <how-to-verify>
**Voraussetzungen:**
1. GatherV2-Electron-App ist installiert und Account-Login funktioniert.
2. App ist beendet (sonst greift der Debug-Flag nicht).

**Schritte:**

1. App mit Debug-Flag starten:
   ```bash
   open -a GatherV2 --args --remote-debugging-port=9222
   ```
2. Im UI in den gewünschten Space einloggen (eigener Avatar im Map-View sichtbar).
3. Pre-Flight prüfen:
   ```bash
   npm run check-cdp
   ```
   Erwartet: `✅ GatherV2-Page erreichbar: https://app.v2.gather.town/...` und Exit-Code 0.
4. Smoke-Test starten:
   ```bash
   npm run test:sink
   ```
5. Innerhalb der ersten 1–2 Sekunden auf den eigenen Avatar im GatherV2-UI schauen.

**Akzeptanz:**
- ✅ Avatar zeigt Custom-Status mit Emoji `🎵` und Text `Daft Punk – Around the World` für ~10 Sekunden.
- ✅ Nach den 10 Sekunden verschwindet der Status (UI ist clean).
- ✅ Skript exitet mit Code 0 (kein `[test-sink] smoke test FAILED`).
- ✅ Logs (`stderr`) zeigen `[gather] CDP pre-flight OK`, `[gather] status set via CDP`, `[gather] status cleared via CDP`.

**Bei Fehler — Diagnose-Reihenfolge:**

1. **`[gather] no GatherV2 page found at localhost:9222`**: App läuft nicht mit Debug-Flag. App komplett beenden und Schritt 1 erneut.
2. **`CDP /json HTTP 4xx/5xx` oder Timeout**: Port belegt oder GatherV2 antwortet nicht. `lsof -i :9222` checken, ggf. anderen Port nutzen (siehe Troubleshooting-Sektion in README).
3. **`runInPage failed: ReferenceError: gatherDev is not defined`**: User ist auf Login-Page, nicht im Space. Im UI in den Space gehen.
4. **`runInPage failed: TypeError: Cannot read properties of undefined`**: GatherV2-App-Update hat die interne API (`gatherDev.Repos.gameSpace.currentSpaceUser`) verschoben. In der DevTools-Konsole (rechtsklick im Renderer → Inspect) `window.gatherDev` ausgeben und Pfad neu mappen — das ist ein Fix in `src/sink/gather.ts > setStatus/clearStatus`.

**Out-of-Scope für diesen Checkpoint:**
- Polling-Loop-Verhalten (gehört zu Phase 3 Verification)
- launchd-Integration (gehört zu Phase 4)
- Reconnect bei App-Restart während laufendem Daemon (deferred zu v2.1)
  </how-to-verify>
  <resume-signal>Antwort `approved`, `failed: <Detail>`, oder `skipped` (kein Zugriff auf laufende GatherV2 verfügbar)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon → localhost CDP | Bridge connected lokal zu `http://localhost:9222/json` und dem WebSocket-Debugger der GatherV2-Electron-App. Der CDP-Server ist NUR an localhost gebunden — keine Netz-Exposure unter normaler macOS-Konfiguration. |
| daemon → GatherV2 Renderer | `Runtime.evaluate` führt JS im Renderer-Kontext der App aus — der Daemon hat damit dieselben Rechte wie die App selbst (eingeloggter Gather-User). |
| GatherV2 ↔ Gather-Backend | Vollständig delegiert an die App. Kein API-Key in der Bridge mehr. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `runInPage(expression)` mit interpoliertem `np.artist`/`np.track` | mitigate | `JSON.stringify` für jeden eingefügten String — schützt gegen Anführungszeichen, Newlines, JS-Injection im Track-Namen. Ohne Stringify würde z.B. `track = "x\");dropDatabase();//"` zur Code-Injection in der GatherV2-Renderer-Page führen. |
| T-05-02 | Information Disclosure | CDP-Port `9222` ist auf macOS lokal, aber jede lokal laufende App kann sich verbinden | accept | Single-User-Tool, lokale macOS-Box. Wer lokalen Code-Execution hat, hat eh schon Vollzugriff (Apple-Music-Steuerung, Keychain etc.). Kein zusätzlicher Vector. |
| T-05-03 | Denial of Service | CDP-Port nicht erreichbar oder GatherV2-Page weg → wiederholte Reconnect-Versuche | mitigate | `AbortSignal.timeout(2000)` pro Fetch verhindert Hänger. Loop hat Try/Catch um jeden Tick (Phase 3) — kein Crash, nur warn-Log. Polling-Intervall 10s drosselt Failure-Rate. |
| T-05-04 | Elevation of Privilege | `Runtime.evaluate` ist effektiv `eval` im Renderer | accept | Wir kontrollieren die expression-Templates (zwei feste, keine User-kontrollierten). Einzige interpolierte Werte sind die JSON.stringify-geschützten artist/track-Strings (siehe T-05-01). |
| T-05-05 | Repudiation | Bridge schreibt Status im Namen des eingeloggten Users | accept | Genau das ist der Use-Case. Audit-Trail fällt in Gathers Verantwortung. |
| T-05-06 | Spoofing | Anderer Prozess könnte sich vor dem Bridge-Start an Port 9222 binden | accept | Kein realistischer Angriffsvector im Single-User-Setup. Im Worst-Case fehlt die `app.v2.gather.town`-Page → Bridge throws "no GatherV2 page found" → kein Status-Set. |
</threat_model>

<verification>
**Plan-Level-Checks** (zusätzlich zu den Per-Task `<verify>`-Gates):

1. **Type-Check**:
   ```bash
   npx tsc -p . --noEmit
   ```
   muss exit 0 nach Task 6 und nach Task 7 sein.

2. **Dep-Cleanup**:
   ```bash
   ! grep -qE '"(@gathertown/gather-game-client|isomorphic-ws)"' package.json && \
     ! test -d node_modules/@gathertown && \
     ! test -d node_modules/isomorphic-ws && \
     test -d node_modules/chrome-remote-interface
   ```

3. **No-stale-imports-Sweep**:
   ```bash
   ! grep -rn "setup-ws\|@gathertown/gather-game-client\|isomorphic-ws" src/ scripts/
   ```
   muss leer sein.

4. **Public-API-Stable** (Smoke-Compile):
   ```bash
   grep -E "connect\(|setStatus\(|clearStatus\(|disconnect\(|get connected" src/sink/gather.ts | wc -l
   ```
   muss mindestens 5 Treffer ergeben (alle fünf Public-API-Mitglieder).

5. **Goal-Backward-Verifikation der must_haves > truths**:
   - "GatherSink schreibt Custom-Status via CDP" → Task 3 + Smoke-Test in Task 9 (optional)
   - "GatherSink leert Status via clearCustomStatus" → Task 3 + Smoke-Test
   - "connect() wirft mit Setup-Anleitung bei missing CDP" → Task 3 verifiziert per `grep "remote-debugging-port" src/sink/gather.ts`
   - "Config ohne Gather-Pflicht-Keys" → Task 4
   - "Deps clean" → Task 1 + Plan-Level-Check 2
   - "setup-ws weg" → Task 2 + Plan-Level-Check 3
   - "tsc clean" → Task 6 + Plan-Level-Check 1
   - "Smoke-Test mit `new GatherSink()`" → Task 5
   - "Loop/Index awaiten Sink" → Task 6
   - "README dokumentiert Setup" → Task 8
</verification>

<success_criteria>
1. `npx tsc -p . --noEmit` exit 0.
2. `package.json` `dependencies` enthält `chrome-remote-interface@^0.33.x`, KEIN `@gathertown/gather-game-client`, `isomorphic-ws`, `ws`.
3. `src/setup-ws.ts` existiert nicht (`! test -e src/setup-ws.ts`).
4. `src/sink/gather.ts` ruft `setCustomStatus({emoji, text, clearCondition: {type: "Never"}})` und `clearCustomStatus()` via CDP `Runtime.evaluate`.
5. `src/config.ts` hat weder `GATHER_API_KEY` noch `GATHER_SPACE_ID` im Schema; hat `GATHER_CDP_PORT` (default `"9222"`) und `GATHER_PAGE_URL_FILTER` (default `"app.v2.gather.town"`) als optionale Felder.
6. `scripts/test-sink.ts` ruft `new GatherSink()` ohne Args und awaitet alle vier Methoden.
7. `src/loop.ts` awaitet `sink.setStatus(np)` und `sink.clearStatus()`.
8. `src/index.ts` awaitet `sink.clearStatus()` im Shutdown-Handler und ruft `new GatherSink({port, pageUrlFilter})`.
9. `scripts/check-cdp.ts` existiert und hat `npm run check-cdp` als Aufruf.
10. README erklärt GatherV2-Start mit `--remote-debugging-port=9222` und nennt `npm run check-cdp` als Diagnose.
11. Alle 8 Code-Tasks haben atomic Commits mit `feat(05-01)`/`chore(05-01)`/`refactor(05-01)`/`docs(05-01)` Präfix.
12. Optional Task 9 (human-verify): visuelle Akzeptanz im echten GatherV2-UI.
</success_criteria>

<output>
After completion, create `.planning/phases/05-cdp-bridge-refactor/05-01-SUMMARY.md` per the GSD summary template:
- Tasks 1–8 mit Commit-Hashes und betroffenen Files.
- Task 9 als deferred (User-Action) oder approved (falls visuell verifiziert) markieren.
- Decisions Made: Per-Call-CDP statt persistent (CONTEXT.md > Decisions), `runInPage` statt `eval` als Methodenname (Security-Reminder), `JSON.stringify` für interpolierte Werte (T-05-01).
- Deviations vom Plan: dokumentieren falls beim Schreiben von Task 3 etwas vom Skelett abgewichen wurde (z.B. `runInPage` als private Methode mit Auslagerung des `pageUrlFilter`-Match — falls so umgesetzt).
- Coverage Statement: SINK-01..05 unter neuer CDP-Implementierung erfüllt; CFG-01..02 weiter erfüllt (Schema-Validation, klare Fehlermeldung bei Fehlkonfiguration), CFG-03..05 unverändert (waren in Phase 1 / Phase 4 erfüllt, Phase 5 ändert nichts daran).
- Next Phase Readiness: Phase 3 Loop kann jetzt gegen GatherV2 laufen, Phase 4 launchd-Plist ist sink-agnostisch — kein Update nötig. Mögliche v2.1-Phase: persistente CDP-Connection mit Reconnect-Logic, Auto-Start-GatherV2-mit-Flag als Login-Item.
</output>
