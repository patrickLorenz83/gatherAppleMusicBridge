---
name: Bug report
about: Daemon doesn't push status, crashes, or behaves unexpectedly
labels: bug
---

**What did you expect to happen?**

**What happened instead?**

**Environment**
- macOS version:
- Node version (`node --version`):
- GatherV2 version (`defaults read /Applications/GatherV2.app/Contents/Info.plist CFBundleShortVersionString`):
- Bridge version (`git log -1 --format=%H` if cloned from source):

**Logs**

Please paste the relevant excerpt from `~/Library/Logs/gather-bridge.log`
and/or `~/Library/Logs/gather-bridge.err` from around the time of the
issue. Pretty-print with `npx pino-pretty` if you like.

```
<paste here>
```

**Steps to reproduce**

1.
2.
3.

**Anything you've tried already?**
